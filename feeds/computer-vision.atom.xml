<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"><title>Michał Chromiak's blog - Computer Vision</title><link href="https://mchromiak.github.io/" rel="alternate"/><link href="/feeds/computer-vision.atom.xml" rel="self"/><id>https://mchromiak.github.io/</id><updated>2023-05-30T19:07:00+02:00</updated><subtitle>Be a fool to become a Polymath.</subtitle><entry><title>DINOv2: How self-supervised visual features transfer across tasks</title><link href="https://mchromiak.github.io/articles/2023/Apr/18/DINOv2-Learning-Robust-Visual-Features-without-Supervision/" rel="alternate"/><published>2023-04-18T19:07:00+02:00</published><updated>2023-05-30T19:07:00+02:00</updated><author><name>Michał Chromiak</name></author><id>tag:mchromiak.github.io,2023-04-18:/articles/2023/Apr/18/DINOv2-Learning-Robust-Visual-Features-without-Supervision/</id><summary type="html">&lt;p&gt;DINOv2 trains Vision Transformer encoders on 142 million curated images without using labels or captions in its pretraining objective. This guide explains how image-level self-distillation, masked patch prediction, feature-spreading regularization, data curation, and distillation combine to produce features that transfer across classification, retrieval, segmentation, and depth estimation.&lt;/p&gt;</summary><content type="html">&lt;p&gt;Image classification needs a compact description of an entire image. Segmentation and depth estimation need a useful description at every patch. Retrieval needs features that place visually related images close together even when style, pose, or background changes.&lt;/p&gt;
&lt;p&gt;DINOv2 tackles these requirements with one Vision Transformer encoder. It combines image-level self-distillation, masked patch prediction, a feature-spreading regularizer, curated data, and engineering at scale. The released family spans ViT-S/14, ViT-B/14, ViT-L/14, and ViT-g/14; the largest backbone has about 1.1 billion parameters.&lt;/p&gt;
&lt;p&gt;Throughout this article, &lt;strong&gt;frozen&lt;/strong&gt; means that downstream training does not change the encoder weights. A &lt;strong&gt;readout&lt;/strong&gt; is the smaller task-specific component that turns the encoder's features into class predictions, segmentation masks, or depth estimates.&lt;/p&gt;
&lt;details class="dinov2-background"&gt;
&lt;summary&gt;Background: From pixels to visual features&lt;/summary&gt;
&lt;p&gt;Imagine a photograph of a horse in a field. Its pixels describe colors and brightness. A &lt;strong&gt;visual feature vector&lt;/strong&gt; is a list of numbers computed from those pixels by an &lt;strong&gt;encoder&lt;/strong&gt;, the part of the network that produces representations. Training makes these numbers useful for comparing images or predicting their contents. Individual coordinates need not have names such as "horse" or "grass"; information can be distributed across many coordinates.&lt;/p&gt;
&lt;p&gt;A Vision Transformer divides the photograph into small &lt;strong&gt;patches&lt;/strong&gt; and maps each patch to a numerical &lt;strong&gt;token&lt;/strong&gt;. Attention lets tokens draw information from other tokens, so a patch representation can depend on the surrounding scene. The encoder produces local patch features as well as a whole-image summary.&lt;/p&gt;
&lt;p&gt;The training recipe gives these features three jobs: &lt;strong&gt;self-distillation&lt;/strong&gt; asks different crops of the photograph to agree through a teacher and student; &lt;strong&gt;masked patch prediction&lt;/strong&gt; asks the student to predict the teacher's features where patches were hidden, rather than repaint their pixels; a &lt;strong&gt;regularizer&lt;/strong&gt; adds a constraint, such as discouraging different images from receiving nearly identical vectors. Data curation determines which photographs the model learns from.&lt;/p&gt;
&lt;/details&gt;
&lt;h2 id="what-without-supervision-means"&gt;What "without supervision" means&lt;a class="headerlink" href="#what-without-supervision-means" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;During pretraining, DINOv2 receives images, crops, and patch masks. It receives no class labels and no paired captions. This differs from CLIP-style pretraining, where text supplies a weak semantic signal. An image-only objective can retain local visual structure that a caption might omit, and it can use collections for which descriptions do not exist.&lt;/p&gt;
&lt;p&gt;The data is still curated. Seed datasets, filtering rules, deduplication, and similarity search determine which images enter the final corpus. These are separate claims: &lt;strong&gt;the training signal is label-free; the data pipeline is deliberately constructed&lt;/strong&gt;.&lt;/p&gt;
&lt;h2 id="the-training-signal-teacher-and-student"&gt;The training signal: teacher and student&lt;a class="headerlink" href="#the-training-signal-teacher-and-student" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;DINOv2 trains a student network against a teacher network across different crops of the same source image. Gradient descent updates the student. An exponential moving average of the student updates the teacher, which provides slowly changing targets.&lt;/p&gt;
&lt;p&gt;Three terms give the representation complementary properties. The DINO term aligns whole-image representations across views. The iBOT term asks masked student patches to recover the teacher's local representation. KoLeo keeps image representations spread through feature space.&lt;/p&gt;
&lt;details class="dinov2-background"&gt;
&lt;summary&gt;Background: How can a teacher learn from its student?&lt;/summary&gt;
&lt;p&gt;During this pretraining stage, the teacher is not an independently trained expert. It starts as a copy of the student. Both process versions of the horse photograph, and a &lt;strong&gt;loss&lt;/strong&gt; measures disagreement between their outputs.&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;Gradient descent&lt;/strong&gt; uses derivatives to determine how small changes to the student's weights would change the loss, then adjusts those weights toward a lower loss. The teacher's output is held fixed during this calculation: gradients do not update the teacher.&lt;/p&gt;
&lt;p&gt;After the student update, the teacher moves a little toward the student's new weights. For an illustrative moving average, each teacher weight could become 99% of its previous value plus 1% of the corresponding student weight. Repeating this blends a history of student weights, with older contributions gradually fading. These percentages illustrate the operation, not the paper's training schedule.&lt;/p&gt;
&lt;p&gt;The teacher therefore changes more slowly than the student, giving it a steadier target. Repeated agreement across crops encourages features that survive changes in the input. Averaging alone is not enough: the balancing and regularization described below discourage the trivial solution of giving every image the same representation.&lt;/p&gt;
&lt;/details&gt;
&lt;h3 id="image-level-dino-loss"&gt;Image-level DINO loss&lt;a class="headerlink" href="#image-level-dino-loss" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Each network uses a special class token to collect information from the whole crop. A small projection head maps that token to a probability distribution over learned prototypes: output coordinates that are learned during training rather than assigned class names. After normalization, &lt;span class="math"&gt;\(p_s\)&lt;/span&gt; and &lt;span class="math"&gt;\(p_t\)&lt;/span&gt; denote the student's and teacher's distributions. Their cross-entropy is:&lt;/p&gt;
&lt;div class="math"&gt;$$
\mathcal{L}_{DINO} = -\sum p_t \log p_s.
$$&lt;/div&gt;
&lt;p&gt;The compact equation leaves two sums implicit: one over prototype coordinates and another over the valid pairs of teacher and student crops. Minimizing it teaches the student which image-level information should remain stable across views.&lt;/p&gt;
&lt;details class="dinov2-background"&gt;
&lt;summary&gt;Background: What do prototypes and a lower loss mean?&lt;/summary&gt;
&lt;p&gt;The projection head compares the crop's features with learned directions called &lt;strong&gt;prototypes&lt;/strong&gt;, then converts the scores to probabilities that add up to one. These prototypes are training coordinates, not predefined object classes. A high score for prototype A does not mean "80% probability of a horse."&lt;/p&gt;
&lt;p&gt;For a toy head with only three prototypes, suppose the teacher assigns the horse crop probabilities &lt;strong&gt;(0.8, 0.1, 0.1)&lt;/strong&gt; over A, B, and C. A student that assigns &lt;strong&gt;(0.8, 0.1, 0.1)&lt;/strong&gt; to another crop agrees with it. A student assigning &lt;strong&gt;(0.1, 0.8, 0.1)&lt;/strong&gt; instead favors B and disagrees where the teacher places most of its weight.&lt;/p&gt;
&lt;p&gt;The cross-entropy equation quantifies that distinction. Each teacher probability weights the negative logarithm of the corresponding student probability. Using natural logarithms, the matching prediction has loss about &lt;strong&gt;0.64&lt;/strong&gt;, while the mismatching one has loss about &lt;strong&gt;2.09&lt;/strong&gt;. A smaller student probability on the teacher's preferred coordinate incurs a larger penalty. Matching a soft target need not give zero loss, because the teacher still spreads some probability across alternatives.&lt;/p&gt;
&lt;p&gt;The iBOT equation below uses the same comparison at masked patch positions: the student predicts a distribution over patch prototypes, not the hidden RGB pixels.&lt;/p&gt;
&lt;/details&gt;
&lt;h3 id="patch-level-ibot-loss"&gt;Patch-level iBOT loss&lt;a class="headerlink" href="#patch-level-ibot-loss" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The image-level term does not directly require detailed patch features, so DINOv2 adds a local prediction task. Some patches are hidden from the student but remain visible to the teacher. At every masked position &lt;span class="math"&gt;\(i\)&lt;/span&gt;, the student predicts the teacher's distribution for the corresponding visible patch:&lt;/p&gt;
&lt;div class="math"&gt;$$
\mathcal{L}_{iBOT} = -\sum_i p_{ti} \log p_{si},
$$&lt;/div&gt;
&lt;p&gt;where &lt;span class="math"&gt;\(i\)&lt;/span&gt; ranges over masked patch positions. DINOv2 gives the image-level DINO objective and patch-level iBOT objective separate learnable projection heads. The original iBOT ablation favored shared heads, but the DINOv2 experiments found separate heads more effective at this scale.&lt;/p&gt;
&lt;h3 id="centering-and-koleo-regularization"&gt;Centering and KoLeo regularization&lt;a class="headerlink" href="#centering-and-koleo-regularization" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;If every image mapped to the same feature, the matching losses would become useless. DINOv2 counters this collapse by normalizing the student with softmax and balancing the teacher's assignments with three iterations of the Sinkhorn-Knopp algorithm.&lt;/p&gt;
&lt;details class="dinov2-background"&gt;
&lt;summary&gt;Background: Why balance assignments across a batch?&lt;/summary&gt;
&lt;p&gt;Agreement alone permits an unhelpful shortcut: the teacher and student could assign every crop to prototype A, regardless of whether it contains a horse, a tree, or a building. They would agree without distinguishing the images. This is one form of &lt;strong&gt;collapse&lt;/strong&gt;.&lt;/p&gt;
&lt;p&gt;Picture a table with one row per crop in a training batch and one column per prototype. Its entries represent assignment weights. &lt;strong&gt;Sinkhorn-Knopp&lt;/strong&gt; alternately rescales rows and columns toward prescribed totals, balancing how much total weight the prototypes receive across the batch. The paper uses three iterations, so this is an approximate balancing procedure.&lt;/p&gt;
&lt;p&gt;Balancing the columns does not require every row to be uniform. One crop can strongly favor A and another B while overall prototype usage remains balanced. Nor does balancing assign human meanings to A and B. Teacher temperature controls how concentrated the assignments are; lower temperatures emphasize stronger scores. Concentration and batch-level balance work together rather than making every crop produce the same flat distribution.&lt;/p&gt;
&lt;p&gt;KoLeo addresses the geometry of the features themselves. Imagine normalized image vectors as points on a sphere. When nearest neighbors crowd together, the negative-log-distance penalty grows; spreading them out reduces it. This complements balancing the projection head's outputs.&lt;/p&gt;
&lt;/details&gt;
&lt;p&gt;DINOv2 also applies the KoLeo regularizer to spread class-token features within a batch. For &lt;span class="math"&gt;\(n\)&lt;/span&gt; feature vectors &lt;span class="math"&gt;\((x_1,\ldots,x_n)\)&lt;/span&gt;, the paper defines:&lt;/p&gt;
&lt;div class="math"&gt;$$
\mathcal{L}_{\mathrm{KoLeo}}
= -\frac{1}{n}\sum_{i=1}^{n}\log(d_{n,i}),
$$&lt;/div&gt;
&lt;p&gt;with&lt;/p&gt;
&lt;div class="math"&gt;$$
d_{n,i} = \min_{j\neq i}\lVert x_i-x_j\rVert.
$$&lt;/div&gt;
&lt;p&gt;The features are &lt;span class="math"&gt;\(\ell_2\)&lt;/span&gt;-normalized before measuring distance. Because the loss increases when nearest neighbors crowd together, minimizing it spreads class-token features across the available space. The ablation connects this term especially strongly to nearest-neighbor retrieval, while the masked iBOT term matters for dense prediction.&lt;/p&gt;
&lt;h3 id="high-resolution-adaptation"&gt;High-resolution adaptation&lt;a class="headerlink" href="#high-resolution-adaptation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Small objects may occupy too few patches at low resolution, while full high-resolution pretraining is expensive. DINOv2 resolves that tradeoff with a short adaptation stage at &lt;span class="math"&gt;\(518\times518\)&lt;/span&gt; pixels near the end of pretraining. The model gets a final period of finer spatial input without paying that cost for the entire run.&lt;/p&gt;
&lt;h2 id="building-lvd-142m"&gt;Building LVD-142M&lt;a class="headerlink" href="#building-lvd-142m" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The training set is large, but size alone is not the recipe. The authors use curated collections to guide the selection of relevant and varied images from a much larger uncurated pool.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="Diagram of curated images guiding retrieval from an embedded and deduplicated web-image pool" src="https://mchromiak.github.io/articles/2023/Apr/18/DINOv2-Learning-Robust-Visual-Features-without-Supervision/img/dinov2_pipeline.png"&gt;
&lt;br&gt;
Figure 1. Construction of LVD-142M. Images in the gray uncurated pool are embedded and deduplicated. Images similar to examples from the orange curated sources are then retrieved and added to the curated collection.&lt;/p&gt;
&lt;p&gt;The pipeline proceeds as follows:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;The authors start with a raw web pool. URL filtering, near-duplicate removal, NSFW filtering, and blurring of identifiable faces reduce it to about 1.2 billion unique images.&lt;/li&gt;
&lt;li&gt;They remove near-duplicates of the test and validation images used in the paper's benchmarks.&lt;/li&gt;
&lt;li&gt;They assemble curated seed sources: ImageNet-22k, the ImageNet-1k training split, Google Landmarks, and several fine-grained datasets.&lt;/li&gt;
&lt;li&gt;A self-supervised ViT-H/16 pretrained on ImageNet-22k embeds both pools. Cosine similarity and k-means then connect uncurated images to the curated sources.&lt;/li&gt;
&lt;li&gt;For a sufficiently large query dataset, the pipeline retrieves four nearest neighbors per query. Smaller sources use cluster-based sampling with balancing limits.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Selection is driven by visual similarity rather than captions, hashtags, or class annotations. The curated seeds nevertheless shape the final distribution: they determine which regions of the 1.2-billion-image pool the retrieval process explores.&lt;/p&gt;
&lt;p&gt;The paper reports that deduplication and retrieval used the Faiss similarity-search library on 20 nodes, each with eight V100-32GB GPUs, and took less than two days to produce LVD-142M.&lt;/p&gt;
&lt;h2 id="scaling-the-model-and-implementation"&gt;Scaling the model and implementation&lt;a class="headerlink" href="#scaling-the-model-and-implementation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The largest model is a ViT-g/14 with about 1.1 billion parameters. Training a teacher and student of that size adds memory, stability, and throughput constraints, so the implementation work is part of the method rather than an incidental detail:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Memory-efficient attention.&lt;/strong&gt; The authors implemented a FlashAttention-style kernel (&lt;a href="https://arxiv.org/abs/2205.14135"&gt;Dao et al., 2022&lt;/a&gt;). To suit GPU kernels, ViT-g uses an embedding dimension of 1536 with 24 heads, or 64 dimensions per head.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Sequence packing.&lt;/strong&gt; Global crops at resolution 224 and local crops at resolution 98 produce sequences of different lengths. DINOv2 concatenates them and uses a block-diagonal attention mask, making the computation equivalent to separate forwards while improving efficiency. The low-level components are provided by Meta's &lt;a href="https://github.com/facebookresearch/xformers"&gt;xFormers&lt;/a&gt; library.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Efficient stochastic depth.&lt;/strong&gt; Rather than calculate every residual branch and mask selected results, the implementation skips computation for dropped residuals. With the reported drop rate of 40%, this saves both compute and memory.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Fully Sharded Data Parallel.&lt;/strong&gt; FSDP shards the student, teacher, and AdamW optimizer states across GPUs. Model shards remain in &lt;code&gt;float32&lt;/code&gt;; backbone weight broadcasts and gradient reductions use &lt;code&gt;float16&lt;/code&gt;, while projection-head gradients are reduced in &lt;code&gt;float32&lt;/code&gt; for stability.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;On the same hardware, this implementation runs about &lt;span class="math"&gt;\(2\times\)&lt;/span&gt; faster than the iBOT implementation while using &lt;span class="math"&gt;\(1/3\)&lt;/span&gt; of the memory. Those figures compare these two implementations under the paper's setup.&lt;/p&gt;
&lt;h3 id="distilling-the-smaller-models"&gt;Distilling the smaller models&lt;a class="headerlink" href="#distilling-the-smaller-models" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The authors train ViT-g/14 from scratch, then use it to teach the smaller ViT-S/14, ViT-B/14, and ViT-L/14 models. This is &lt;a href="https://arxiv.org/abs/1503.02531"&gt;knowledge distillation&lt;/a&gt;: transfer the behavior of a large teacher into a cheaper student instead of repeating the complete pretraining recipe for every size.&lt;/p&gt;
&lt;p&gt;For this stage, the teacher is fixed, masking and stochastic depth are removed, the iBOT loss is applied on the two global crops, and an exponential moving average of the student becomes the released model. In the paper's ablation, the distilled ViT-L/14 beats a ViT-L/14 trained from scratch across all 12 reported benchmarks.&lt;/p&gt;
&lt;h2 id="how-to-read-the-experiments"&gt;How to read the experiments&lt;a class="headerlink" href="#how-to-read-the-experiments" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The paper evaluates classification, domain generalization, fine-grained recognition, video action recognition, instance retrieval, semantic segmentation, and monocular depth estimation. These results use three distinct protocols:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Non-parametric evaluation:&lt;/strong&gt; compare frozen features directly, as in nearest-neighbor retrieval.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Linear evaluation:&lt;/strong&gt; freeze the backbone and train a linear classifier on its output.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Dense prediction:&lt;/strong&gt; freeze most or all of the backbone and train a decoder or adapter that converts patch features into segmentation or depth predictions.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;This distinction matters because "frozen backbone" describes the encoder, not the entire downstream system. Architecture, pretraining data, input resolution, and readout capacity all belong beside a benchmark number.&lt;/p&gt;
&lt;h3 id="image-classification"&gt;Image classification&lt;a class="headerlink" href="#image-classification" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;With a frozen backbone and a trained linear classifier at resolution 224, DINOv2 ViT-g/14 reaches 86.5% ImageNet-1k top-1 accuracy. In the same evaluation table:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;iBOT ViT-L/16 reaches 82.3%, so the reported improvement over the previous SSL result is 4.2 percentage points;&lt;/li&gt;
&lt;li&gt;OpenCLIP ViT-G/14 reaches 86.2%; and&lt;/li&gt;
&lt;li&gt;EVA-CLIP ViT-g/14 reaches 86.4%.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Under this linear-evaluation protocol, DINOv2 is competitive with and narrowly ahead of these weakly supervised baselines. Results vary by dataset, as the next transfer experiments show.&lt;/p&gt;
&lt;p&gt;Supervised end-to-end fine-tuning raises ViT-g/14 from 86.5% to 88.5% at resolution 224 and from 86.7% to 88.9% at resolution 448. Frozen features provide a strong starting point; fine-tuning still adds 2.0 and 2.2 percentage points respectively.&lt;/p&gt;
&lt;h3 id="classification-beyond-imagenet-and-video"&gt;Classification beyond ImageNet and video&lt;a class="headerlink" href="#classification-beyond-imagenet-and-video" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The paper trains linear classifiers on frozen DINOv2 features for iNaturalist, Places205, and 12 additional transfer datasets. DINOv2 is particularly strong on fine-grained categories, but OpenCLIP remains better on some benchmarks such as SUN and Stanford Cars.&lt;/p&gt;
&lt;p&gt;For video action recognition, the frozen image encoder processes eight frames independently. The evaluation averages their features for UCF-101 and Kinetics-400, while Something-Something v2 concatenates them to preserve temporal order before training the linear classifier. The experiment tests whether frame-level visual features support action recognition; the pretraining objective itself remains image-based.&lt;/p&gt;
&lt;h3 id="instance-retrieval"&gt;Instance retrieval&lt;a class="headerlink" href="#instance-retrieval" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Instance retrieval is the paper's direct, non-parametric use of the representation. The system computes one feature per image and ranks database images by cosine similarity to the query; there is no task-specific retrieval head. On Oxford-Hard, the strongest reported DINOv2 result improves by roughly 41 mAP points over the selected SSL baseline and 34 points over OpenCLIP.&lt;/p&gt;
&lt;p&gt;The photograph on the right is the query. The numbered panels on the left are its ten highest-ranked database matches. The medium, color, pose, and background vary, yet every result depicts horses; several also retain the side-on pose or a multi-horse composition. The figure makes the behavior visible, while the Oxford-Hard score above provides the quantitative test.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="Query photograph of two horses beside ten ranked retrieved horse artworks" src="https://mchromiak.github.io/articles/2023/Apr/18/DINOv2-Learning-Robust-Visual-Features-without-Supervision/img/dinoV2_instance_retrieval.png"&gt;
&lt;br&gt;
Figure 2. Instance-retrieval example. Right: a query photograph of a gray horse and a brown horse in a field. Left: ten retrieved horse images, ranked 1-10 by similarity in the frozen DINOv2 feature space. The matches span photographs, paintings, and line drawings while retaining the subject and some pose or composition cues.&lt;/p&gt;
&lt;h3 id="semantic-segmentation"&gt;Semantic segmentation&lt;a class="headerlink" href="#semantic-segmentation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Semantic segmentation assigns a class to every pixel. This directly tests whether the patch tokens preserve object boundaries and local category information. The paper keeps the backbone frozen and compares three increasingly capable predictors trained with semantic labels:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;a linear layer on patch tokens;&lt;/li&gt;
&lt;li&gt;a stronger multiscale linear setup using the last four transformer layers, a larger input, and multiscale inference; and&lt;/li&gt;
&lt;li&gt;a trained ViT-Adapter plus Mask2Former head on top of the frozen backbone.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The multiscale ViT-g/14 setup reaches 53.0 mean intersection-over-union (mIoU) on ADE20K, close to MAE with end-to-end UperNet fine-tuning at 53.6. Adding the ViT-Adapter and Mask2Former raises DINOv2 to 60.2 mIoU while keeping 66% of the weights frozen; the cited state of the art reaches 62.9. The 7.2-point gain from the stronger readout measures how much more spatial information it can extract from the same frozen backbone.&lt;/p&gt;
&lt;script src="https://vjs.zencdn.net/7.11.4/video.min.js"&gt;&lt;/script&gt;
&lt;video id="dinov2-segmentation-comparison" class="video-js vjs-theme-sea vjs-big-play-centered" controls preload="metadata" autoplay loop muted&gt;
  &lt;source src="../img/dinoVSdino2.webm" type="video/webm"&gt;
  &lt;p class="vjs-no-js"&gt;
    To view this video, enable JavaScript and use a browser that
    &lt;a href="https://videojs.com/html5-video-support/" target="_blank" rel="noopener"&gt;supports HTML5 video&lt;/a&gt;.
  &lt;/p&gt;
&lt;/video&gt;

&lt;p align="center"&gt;Figure 3. Side-by-side qualitative comparison of segmentation outputs built from DINO and DINOv2 features. Cleaner boundaries and more stable regions make the improvement visible across frames. (&lt;a href="https://github.com/facebookresearch/dinov2/blob/main/README.md"&gt;Source&lt;/a&gt;)&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="Semantic-segmentation overlay following two horses in a grassy field" src="https://mchromiak.github.io/articles/2023/Apr/18/DINOv2-Learning-Robust-Visual-Features-without-Supervision/img/dinoV2_semantic_segm.png"&gt;
&lt;br&gt;
Figure 4. A colored segmentation overlay follows the contours of both horses while leaving the field and sky as background. A task-specific predictor supplies the semantic output from frozen DINOv2 patch features.&lt;/p&gt;
&lt;h3 id="monocular-depth-estimation"&gt;Monocular depth estimation&lt;a class="headerlink" href="#monocular-depth-estimation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Monocular depth estimation predicts scene depth from a single image. The evaluation again freezes the DINOv2 backbone, then compares how much geometry three readouts can recover: a linear head over one transformer layer, a linear head over four layers, and a DPT decoder.&lt;/p&gt;
&lt;p&gt;With DPT, DINOv2 ViT-g/14 reports root-mean-square error (RMSE) of 0.279 on NYU Depth v2, 2.11 on KITTI, and 0.338 when transferring from NYU Depth v2 to SUN RGB-D. Lower RMSE is better. The corresponding reference results in the paper are 0.330, 2.10, and 0.421: DINOv2 improves the NYU and transfer results and is effectively level with the KITTI reference.&lt;/p&gt;
&lt;video id="dinov2-depth-estimation" class="video-js vjs-theme-sea vjs-big-play-centered" controls preload="metadata" autoplay loop muted&gt;
  &lt;source src="../img/vDinoV2_depth_estimation.webm" type="video/webm"&gt;
  &lt;p class="vjs-no-js"&gt;
    To view this video, enable JavaScript and use a browser that
    &lt;a href="https://videojs.com/html5-video-support/" target="_blank" rel="noopener"&gt;supports HTML5 video&lt;/a&gt;.
  &lt;/p&gt;
&lt;/video&gt;

&lt;p align="center"&gt;Figure 5. Video examples pair input frames with color-coded depth predictions from a trained depth head operating on DINOv2 features.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="Photograph of a horse beside its color-coded monocular-depth prediction" src="https://mchromiak.github.io/articles/2023/Apr/18/DINOv2-Learning-Robust-Visual-Features-without-Supervision/img/dinoV2_depth_estimation.png"&gt;
&lt;br&gt;
Figure 6. Left: the input photograph. Right: the predicted relative-depth map, where the horse and nearby ground separate from the more distant scene. The depth head is trained; the DINOv2 backbone remains frozen.&lt;/p&gt;
&lt;h2 id="reading-the-patch-feature-visualizations"&gt;Reading the patch-feature visualizations&lt;a class="headerlink" href="#reading-the-patch-feature-visualizations" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The paper uses principal component analysis (PCA) to turn high-dimensional patch features into a picture:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;Compute PCA jointly over the patch tokens for each column of related images.&lt;/li&gt;
&lt;li&gt;Threshold the first principal component to remove the background.&lt;/li&gt;
&lt;li&gt;Map three subsequent components to red, green, and blue.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The colors are therefore coordinates in feature space, not class labels. When the same colors recur on the wings of different birds or the body of a horse in different styles, those patches vary along similar feature directions.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="Related photographs and drawings paired with PCA-colored DINOv2 patch-feature maps" src="https://mchromiak.github.io/articles/2023/Apr/18/DINOv2-Learning-Robust-Visual-Features-without-Supervision/img/dinov2_patch_embeddings.png"&gt;
&lt;br&gt;
Figure 7. Each group pairs related source images with PCA-colored patch maps. Wings align across birds and aircraft, body regions align across elephants and horses, and vehicle parts align across drawings and photographs despite changes in pose, style, and category.&lt;/p&gt;
&lt;p&gt;For explicit patch matching, the authors compute Euclidean distances between patch features from two images, solve an assignment problem, and retain salient matches with non-maximum suppression. Examples connect wings to wings, heads to heads, and functionally similar regions across photographs, drawings, poses, and even different objects.&lt;/p&gt;
&lt;p&gt;Together, the PCA views and explicit matches show that local DINOv2 features encode useful correspondences across domains. Calling this "understanding object parts" is reasonable shorthand for the measured behavior: corresponding regions occupy corresponding locations in feature space. Symbolic or causal understanding would require a different experiment.&lt;/p&gt;
&lt;h2 id="applications-and-appropriate-usage"&gt;Applications and appropriate usage&lt;a class="headerlink" href="#applications-and-appropriate-usage" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The experiments suggest a practical pattern: start with the frozen encoder, measure how far a simple readout gets, and add a stronger decoder or fine-tuning only when the task needs it. Suitable uses include:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;image and video classification with a trained probe;&lt;/li&gt;
&lt;li&gt;non-parametric instance retrieval;&lt;/li&gt;
&lt;li&gt;semantic segmentation with a trained linear head or decoder;&lt;/li&gt;
&lt;li&gt;monocular depth estimation with a trained head;&lt;/li&gt;
&lt;li&gt;object detection and other dense tasks using adapters or decoders; and&lt;/li&gt;
&lt;li&gt;specialized domains where labels or captions are scarce.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Cellular imaging is one plausible specialized domain because useful visual structure may be abundant while annotations are scarce. The paper presents this as an application direction rather than an evaluated result; it does not train a cellular foundation model or report a biological-discovery experiment.&lt;/p&gt;
&lt;h2 id="limitations-bias-and-cost"&gt;Limitations, bias, and cost&lt;a class="headerlink" href="#limitations-bias-and-cost" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The paper reports five practical limitations:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Geographical and income bias remains.&lt;/strong&gt; On Dollar Street, the ViT-g model has a 25.7-point performance gap between Africa and Europe and a 31.7-point gap between low- and high-income households. It improves on the selected SEERv2 baseline but remains biased toward wealthier households in Western regions.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;The human-attribute analysis is limited.&lt;/strong&gt; A linear classifier showed no clear harmful-label pattern across the evaluated gender, skin-tone, and age groups, but the authors explicitly state that a more thorough analysis could reveal additional flaws.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Training is expensive.&lt;/strong&gt; Under the paper's standardized assumptions, reproducing one DINOv2-g run requires 22,016 A100 GPU-hours, about 9.7 MWh, and an estimated 3.7 tonnes of CO2 equivalent. The estimate covers GPU electricity and excludes hardware manufacturing and disposal.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Correspondence is narrower than general semantic reasoning.&lt;/strong&gt; The PCA and patch-matching examples measure consistent local features; they do not test a general theory of object-part understanding.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;The representation is not language-aligned.&lt;/strong&gt; Image-only pretraining does not provide CLIP-style open-vocabulary labels. Applications that need named concepts must add a supervised or language-alignment component.&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="future-research"&gt;Future research&lt;a class="headerlink" href="#future-research" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The authors propose that object-part and scene-geometry properties may continue to improve with model and data scale, drawing an analogy to emergent abilities in language models (&lt;a href="https://arxiv.org/abs/2206.07682"&gt;Wei et al., 2022&lt;/a&gt;). The current experiments motivate that hypothesis but do not trace a scaling curve for those properties.&lt;/p&gt;
&lt;p&gt;They also suggest feeding frozen visual features into a language-enabled system as visual tokens. Such a system would need an additional alignment or multimodal training stage because the original DINOv2 representation is image-only.&lt;/p&gt;
&lt;h2 id="conclusions"&gt;Conclusions&lt;a class="headerlink" href="#conclusions" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;DINOv2's contribution is the complete recipe: DINO aligns image-level views, iBOT develops local patch features, KoLeo spreads global representations, LVD-142M supplies curated scale, and the implementation makes a 1.1-billion-parameter teacher trainable. Distillation transfers that representation into smaller models.&lt;/p&gt;
&lt;p&gt;The experiments show where the work resides in a downstream system. The frozen backbone provides transferable image and patch features. Retrieval uses those features directly; classification, segmentation, and depth estimation add a trained readout, and full fine-tuning can improve them further.&lt;/p&gt;
&lt;h2 id="references-and-resources"&gt;References and resources&lt;a class="headerlink" href="#references-and-resources" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The primary reference is &lt;strong&gt;&lt;a href="https://arxiv.org/abs/2304.07193"&gt;"DINOv2: Learning Robust Visual Features without Supervision"&lt;/a&gt;&lt;/strong&gt; by Maxime Oquab and colleagues. It was submitted to arXiv on 14 April 2023, revised in February 2024, and published in &lt;em&gt;Transactions on Machine Learning Research&lt;/em&gt; (TMLR) in 2024. The &lt;a href="https://openreview.net/forum?id=a68SUt6zFt"&gt;OpenReview record&lt;/a&gt; contains the publication history, and the authors provide the &lt;a href="https://github.com/facebookresearch/dinov2"&gt;official implementation and checkpoints&lt;/a&gt;.&lt;/p&gt;
&lt;p&gt;DINOv2 builds on a line of self-supervised visual learning that includes &lt;a href="../../../../2021/May/03/DINO-Emerging-Properties-in-Self-Supervised-Vision-Transformers"&gt;DINO&lt;/a&gt;, &lt;a href="https://arxiv.org/abs/2111.07832"&gt;iBOT&lt;/a&gt;, &lt;a href="../../../../2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners"&gt;MAE&lt;/a&gt;, &lt;a href="https://arxiv.org/abs/2202.08360"&gt;SEERv2&lt;/a&gt;, &lt;a href="https://arxiv.org/abs/2204.07141"&gt;MSN&lt;/a&gt;, &lt;a href="https://arxiv.org/abs/2106.09785"&gt;EsViT&lt;/a&gt;, and &lt;a href="https://arxiv.org/abs/2203.14415"&gt;Mugs&lt;/a&gt;. Its weakly supervised comparison set includes &lt;a href="https://arxiv.org/abs/2103.00020"&gt;CLIP&lt;/a&gt;, &lt;a href="https://github.com/mlfoundations/open_clip"&gt;OpenCLIP&lt;/a&gt;, and &lt;a href="https://arxiv.org/abs/2201.08371"&gt;SWAG&lt;/a&gt;.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/2304.07193"&gt;DINOv2 paper on arXiv&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://openreview.net/forum?id=a68SUt6zFt"&gt;DINOv2 publication record on OpenReview&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://github.com/facebookresearch/dinov2"&gt;Official Meta AI implementation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://github.com/facebookresearch/dinov2/blob/main/MODEL_CARD.md#how-to-get-started-with-the-model"&gt;Official model card and PyTorch Hub checkpoints&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://ai.facebook.com/blog/dino-v2-computer-vision-self-supervised-learning/"&gt;Meta AI introduction to DINOv2&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://paperswithcode.com/paper/dinov2-learning-robust-visual-features"&gt;DINOv2 on Papers with Code&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;Demos: &lt;a href="https://dinov2.metademolab.com/demos?category=depth"&gt;depth estimation&lt;/a&gt;, &lt;a href="https://dinov2.metademolab.com/demos?category=segmentation"&gt;semantic segmentation&lt;/a&gt;, and &lt;a href="https://dinov2.metademolab.com/demos?category=retrieval"&gt;instance retrieval&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;script type="text/javascript"&gt;if (!document.getElementById('mathjaxscript_pelican_#%@#$@#')) {
    var align = "center",
        indent = "0em",
        linebreak = "false";

    if (false) {
        align = (screen.width &lt; 768) ? "left" : align;
        indent = (screen.width &lt; 768) ? "0em" : indent;
        linebreak = (screen.width &lt; 768) ? 'true' : linebreak;
    }

    var mathjaxscript = document.createElement('script');
    mathjaxscript.id = 'mathjaxscript_pelican_#%@#$@#';
    mathjaxscript.type = 'text/javascript';
    mathjaxscript.src = 'https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.3/latest.js?config=TeX-AMS-MML_HTMLorMML';

    var configscript = document.createElement('script');
    configscript.type = 'text/x-mathjax-config';
    configscript[(window.opera ? "innerHTML" : "text")] =
        "MathJax.Hub.Config({" +
        "    config: ['MMLorHTML.js']," +
        "    TeX: { extensions: ['AMSmath.js','AMSsymbols.js','noErrors.js','noUndefined.js'], equationNumbers: { autoNumber: 'none' } }," +
        "    jax: ['input/TeX','input/MathML','output/HTML-CSS']," +
        "    extensions: ['tex2jax.js','mml2jax.js','MathMenu.js','MathZoom.js']," +
        "    displayAlign: '"+ align +"'," +
        "    displayIndent: '"+ indent +"'," +
        "    showMathMenu: true," +
        "    messageStyle: 'normal'," +
        "    tex2jax: { " +
        "        inlineMath: [ ['\\\\(','\\\\)'] ], " +
        "        displayMath: [ ['$$','$$'] ]," +
        "        processEscapes: true," +
        "        preview: 'TeX'," +
        "    }, " +
        "    'HTML-CSS': { " +
        "        availableFonts: ['STIX', 'TeX']," +
        "        preferredFont: 'STIX'," +
        "        styles: { '.MathJax_Display, .MathJax .mo, .MathJax .mi, .MathJax .mn': {color: 'inherit ! important'} }," +
        "        linebreaks: { automatic: "+ linebreak +", width: '90% container' }," +
        "    }, " +
        "}); " +
        "if ('default' !== 'default') {" +
            "MathJax.Hub.Register.StartupHook('HTML-CSS Jax Ready',function () {" +
                "var VARIANT = MathJax.OutputJax['HTML-CSS'].FONTDATA.VARIANT;" +
                "VARIANT['normal'].fonts.unshift('MathJax_default');" +
                "VARIANT['bold'].fonts.unshift('MathJax_default-bold');" +
                "VARIANT['italic'].fonts.unshift('MathJax_default-italic');" +
                "VARIANT['-tex-mathit'].fonts.unshift('MathJax_default-italic');" +
            "});" +
            "MathJax.Hub.Register.StartupHook('SVG Jax Ready',function () {" +
                "var VARIANT = MathJax.OutputJax.SVG.FONTDATA.VARIANT;" +
                "VARIANT['normal'].fonts.unshift('MathJax_default');" +
                "VARIANT['bold'].fonts.unshift('MathJax_default-bold');" +
                "VARIANT['italic'].fonts.unshift('MathJax_default-italic');" +
                "VARIANT['-tex-mathit'].fonts.unshift('MathJax_default-italic');" +
            "});" +
        "}";

    (document.body || document.getElementsByTagName('head')[0]).appendChild(configscript);
    (document.body || document.getElementsByTagName('head')[0]).appendChild(mathjaxscript);
}
&lt;/script&gt;</content><category term="Computer Vision"/><category term="Representation Learning"/><category term="Self-Supervision (SSL)"/><category term="CV"/><category term="ViT"/><category term="GPT"/><category term="WSL"/><category term="Weak-supervision"/><category term="Scalability"/></entry><entry><title>🎭 Masked autoencoder (MAE) for visual representation learning. From the author of ResNet.</title><link href="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/" rel="alternate"/><published>2021-11-14T11:18:00+01:00</published><updated>2021-12-26T11:18:00+01:00</updated><author><name>Michał Chromiak</name></author><id>tag:mchromiak.github.io,2021-11-14:/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/</id><summary type="html">&lt;p&gt;A masked autoencoder (MAE) learns visual representations by reconstructing missing image patches from a small visible subset. It divides an image into regular non-overlapping patches, samples patches uniformly without replacement, removes the masked patches before the encoder, and inserts learned mask tokens only for the lightweight decoder. With a 75% masking ratio, the encoder processes just 25% of the patches. This asymmetric design reduces training time and memory, enabling ViT-Large and ViT-Huge models to scale on ImageNet-1K. A ViT-Huge model pretrained for 1600 epochs and fine-tuned at 448-pixel resolution reaches 87.8% ImageNet-1K top-1 accuracy.&lt;/p&gt;</summary><content type="html">&lt;p align="center"&gt;&lt;img alt="MAE reconstructions for COCO validation images" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/HeaderCoCoResults.png"&gt;
Figure 1. COCO validation images: masked input | MAE reconstruction | ground truth. The MAE model was pretrained on ImageNet-1K, not COCO.
&lt;br&gt;&lt;/p&gt;
&lt;h4 id="the-mae-scalable-learning-paper-explained"&gt;The MAE scalable-learning paper explained&lt;a class="headerlink" href="#the-mae-scalable-learning-paper-explained" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;This article explains &lt;strong&gt;&lt;a href="https://openaccess.thecvf.com/content/CVPR2022/html/He_Masked_Autoencoders_Are_Scalable_Vision_Learners_CVPR_2022_paper.html"&gt;"Masked Autoencoders Are Scalable Vision Learners"&lt;/a&gt;&lt;/strong&gt; by Kaiming He, Xinlei Chen, Saining Xie, Yanghao Li, Piotr Dollár, and Ross Girshick. The work first appeared on arXiv on November 11, 2021 and was published at CVPR 2022.&lt;/p&gt;
&lt;h4 id="tldr"&gt;TL;DR&lt;a class="headerlink" href="#tldr" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;MAE uses an asymmetric encoder-decoder architecture. With the default 75% masking ratio, the encoder receives only the visible 25% of image patches and no mask tokens.&lt;/li&gt;
&lt;li&gt;After the encoder, a lightweight decoder receives the encoded visible patches plus learned mask tokens placed at the missing positions.&lt;/li&gt;
&lt;li&gt;The decoder predicts the RGB pixel values of every patch, but the mean squared error (MSE) training loss is computed only over masked patches.&lt;/li&gt;
&lt;li&gt;The default decoder uses less than 10% of the encoder's computation per token. It is used only during pretraining and discarded afterward.&lt;/li&gt;
&lt;li&gt;For recognition, the encoder processes the complete, uncorrupted image.&lt;/li&gt;
&lt;li&gt;Excluding mask tokens from the encoder improves ImageNet linear-probe accuracy from 59.6% to 73.5%, a 13.9 percentage-point gain. Fine-tuning accuracy improves from 84.2% to 84.9%.&lt;/li&gt;
&lt;li&gt;Random masking works better than the block-wise and grid-wise alternatives evaluated in the paper.&lt;/li&gt;
&lt;li&gt;MAE does not require two augmented views or negative examples. Its default recipe nevertheless uses random resized cropping and horizontal flipping.&lt;/li&gt;
&lt;li&gt;Under end-to-end ImageNet-1K fine-tuning, MAE outperforms the DINO, MoCo v3, and BEiT results reported in the paper's comparison table. This claim does not extend to every evaluation protocol.&lt;/li&gt;
&lt;li&gt;A ViT-H/14 pretrained for 1600 epochs and fine-tuned at 448-pixel resolution reaches 87.8% ImageNet-1K top-1 accuracy using no external pretraining data.&lt;/li&gt;
&lt;li&gt;The paper reports that MAE is 3.5 times faster per pretraining epoch than BEiT.&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id="contributions-of-the-paper"&gt;Contributions of the paper&lt;a class="headerlink" href="#contributions-of-the-paper" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;A high masking ratio turns pixel reconstruction into a challenging self-supervised task while reducing the encoder's workload.&lt;/li&gt;
&lt;li&gt;An asymmetric architecture keeps mask tokens out of the large encoder and handles the full token sequence only in a lightweight decoder.&lt;/li&gt;
&lt;li&gt;Direct pixel prediction is competitive with discrete-token targets. Normalizing pixels within each patch improves representation quality.&lt;/li&gt;
&lt;li&gt;MAE works with modest image augmentation. The default is random resized cropping with horizontal flipping; even the center-crop, no-flip ablation remains reasonably effective.&lt;/li&gt;
&lt;li&gt;The efficient recipe scales from ViT-Base to ViT-Huge and transfers well to classification, object detection, instance segmentation, and semantic segmentation.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="motivation"&gt;Motivation&lt;a class="headerlink" href="#motivation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Masking has been highly successful in natural language processing, especially in BERT. Applying related ideas to computer vision was historically less straightforward because convolutional architectures do not naturally operate on sequences of independently removable patches. Vision Transformers (ViTs) make the construction simple: divide an image into patches, hide a subset, and learn to predict the missing content.&lt;/p&gt;
&lt;p&gt;Images and language differ in an important way. Language is semantically dense: a missing word generally carries substantial meaning. Images have strong spatial redundancy, so a model can often infer nearby pixels from texture or local continuity. MAE therefore masks a large random fraction of the image, typically 75%, to reduce this redundancy and require a more holistic understanding of objects and scenes.&lt;/p&gt;
&lt;p&gt;The reconstruction target is deliberately low-level: pixel values rather than class labels or discrete semantic tokens. The decoder specializes in reconstruction, while the encoder is expected to learn a representation useful for later recognition tasks.&lt;/p&gt;
&lt;h3 id="objective"&gt;Objective&lt;a class="headerlink" href="#objective" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The objective is to pretrain a reusable visual encoder without labels. After pretraining, the decoder is removed and the encoder is transferred to downstream tasks through linear probing, partial fine-tuning, or full fine-tuning.&lt;/p&gt;
&lt;h3 id="background"&gt;Background&lt;a class="headerlink" href="#background" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;ResNet has been highly influential in computer vision and has served as the backbone for self-supervised systems such as BYOL, MoCo, and SimCLR.&lt;sup id="sf-Masked-Autoencoders-Are-Scalable-Vision-Learners-1-back"&gt;&lt;a href="#sf-Masked-Autoencoders-Are-Scalable-Vision-Learners-1" class="simple-footnote" title="The first author and project lead of MAE, Kaiming He, was also the first author of Deep Residual Learning for Image Recognition and Momentum Contrast for Unsupervised Visual Representation Learning."&gt;1&lt;/a&gt;&lt;/sup&gt;&lt;/p&gt;
&lt;p&gt;Earlier masked-image approaches often used convolutional networks or more complicated prediction targets. ViT simplified patch-based self-supervision by representing an image directly as a sequence of patch tokens.&lt;/p&gt;
&lt;p&gt;&lt;a href="https://arxiv.org/abs/2106.08254"&gt;BEiT: BERT Pre-Training of Image Transformers&lt;/a&gt;, first submitted in June 2021, predicts discrete visual tokens for masked patches. Those targets are produced by a discrete variational autoencoder (dVAE). MAE instead predicts pixels. The paper finds that normalized-pixel targets are statistically comparable to dVAE-token targets across the evaluated ImageNet, COCO, and ADE20K settings, without requiring a separately pretrained tokenizer.&lt;/p&gt;
&lt;h3 id="how-mae-works"&gt;How MAE works&lt;a class="headerlink" href="#how-mae-works" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;MAE is an autoencoder: an encoder maps the observed input to a latent representation, and a decoder reconstructs the missing part of the input. Its defining feature is that the large encoder operates only on visible patches.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="MAE architecture" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/MaskedAE.png"&gt;
Figure 1a. MAE architecture. This illustration shows fewer than 25% visible patches; the paper's default configuration uses exactly 25% visible patches and masks 75%.
&lt;br&gt;&lt;/p&gt;
&lt;h4 id="patch-sampling"&gt;Patch sampling&lt;a class="headerlink" href="#patch-sampling" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;An image of height &lt;span class="math"&gt;\(H\)&lt;/span&gt;, width &lt;span class="math"&gt;\(W\)&lt;/span&gt;, and &lt;span class="math"&gt;\(C\)&lt;/span&gt; channels is divided into &lt;span class="math"&gt;\(N=HW/p^2\)&lt;/span&gt; regular, non-overlapping patches of size &lt;span class="math"&gt;\(p\times p\)&lt;/span&gt;. For each image, MAE samples visible patches uniformly without replacement. The remaining patches are removed from the encoder input.&lt;/p&gt;
&lt;p&gt;The default mask ratio is 75%, so the encoder processes exactly 25% of the patch tokens. Other mask ratios are evaluated experimentally; 75% provides the best linear-probe result and is near the top of the fine-tuning range.&lt;/p&gt;
&lt;h4 id="encoder-and-decoder"&gt;Encoder and decoder&lt;a class="headerlink" href="#encoder-and-decoder" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The encoder is a standard ViT with linear patch projection, positional embeddings, and Transformer blocks. It receives visible patch embeddings and a class token, but no mask tokens. Processing a quarter of the normal patch sequence substantially reduces computation and memory.&lt;/p&gt;
&lt;p&gt;The decoder receives:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;the encoder's representations of visible patches;&lt;/li&gt;
&lt;li&gt;copies of a shared learned mask-token vector for missing positions; and&lt;/li&gt;
&lt;li&gt;positional embeddings that identify every patch location.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The tokens are restored to their original spatial order before passing through the decoder's Transformer blocks. The decoder can be designed independently of the encoder, but its design is not irrelevant: decoder depth strongly affects linear probing, while fine-tuning is less sensitive to it.&lt;/p&gt;
&lt;p&gt;The default decoder has eight Transformer blocks with width 512. It uses about 9% of the encoder's FLOPs per token, which the paper summarizes as less than 10%. Because only the lightweight decoder processes the full sequence, the overall pretraining system is efficient.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="Masked-image reconstruction examples" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/ImgValid80Perc_39out196.png"&gt;
Figure 2. ImageNet validation examples evaluated with an 80% mask ratio: 39 of 196 patches are visible. Masked input | reconstruction | ground truth.
&lt;br&gt;&lt;/p&gt;
&lt;h4 id="reconstruction-target-and-loss"&gt;Reconstruction target and loss&lt;a class="headerlink" href="#reconstruction-target-and-loss" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;For every patch, the decoder outputs a vector with &lt;span class="math"&gt;\(p^2C\)&lt;/span&gt; channels: one value for every pixel and color channel in that patch. With RGB &lt;span class="math"&gt;\(16\times16\)&lt;/span&gt; patches, the output dimension is &lt;span class="math"&gt;\(16^2\times3=768\)&lt;/span&gt; per patch token. The number of decoder output tokens, not the number of output channels, corresponds to the number of image patches.&lt;/p&gt;
&lt;p&gt;Let &lt;span class="math"&gt;\(M\)&lt;/span&gt; denote the set of masked patch indices, &lt;span class="math"&gt;\(y_i\in\mathbb{R}^{p^2C}\)&lt;/span&gt; the target pixels for patch &lt;span class="math"&gt;\(i\)&lt;/span&gt;, and &lt;span class="math"&gt;\(\hat{y}_i\)&lt;/span&gt; the corresponding prediction. The paper defines MSE over masked patches in prose. Written explicitly, the official implementation computes:&lt;/p&gt;
&lt;div class="math"&gt;$$
L_{\mathrm{MAE}} =
\frac{1}{|M|}
\sum_{i\in M}
\frac{1}{p^2C}
\left\|\hat{y}_i-y_i\right\|_2^2.
$$&lt;/div&gt;
&lt;p&gt;No loss is applied to visible patches. The authors report that computing loss over all patches slightly decreases downstream accuracy.&lt;/p&gt;
&lt;p&gt;The paper also evaluates per-patch normalized targets:&lt;/p&gt;
&lt;div class="math"&gt;$$
\tilde{y}_i =
\frac{y_i-\mu_i}
{\sqrt{\sigma_i^2+\varepsilon}},
$$&lt;/div&gt;
&lt;p&gt;where &lt;span class="math"&gt;\(\mu_i\)&lt;/span&gt; and &lt;span class="math"&gt;\(\sigma_i^2\)&lt;/span&gt; are the mean and variance of the pixel values within patch &lt;span class="math"&gt;\(i\)&lt;/span&gt;. Normalized pixels improve the ImageNet fine-tuning result from 84.9% to 85.4% in the 800-epoch ViT-L ablation.&lt;/p&gt;
&lt;h4 id="processing-steps"&gt;Processing steps&lt;a class="headerlink" href="#processing-steps" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;ol&gt;
&lt;li&gt;Divide each image into patches, linearly embed them, and add positional embeddings.&lt;/li&gt;
&lt;li&gt;Randomly shuffle the patch-token list and retain the visible fraction.&lt;/li&gt;
&lt;li&gt;Process only the visible tokens with the encoder.&lt;/li&gt;
&lt;li&gt;Project the encoded tokens to the decoder dimension and append learned mask tokens.&lt;/li&gt;
&lt;li&gt;Unshuffle the full list so visible representations and mask tokens return to their spatial positions.&lt;/li&gt;
&lt;li&gt;Add decoder positional embeddings and process the full sequence with the decoder.&lt;/li&gt;
&lt;li&gt;Predict the pixel vector for every patch and average MSE only over masked patches.&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;The shuffle and unshuffle operations add negligible overhead and require no specialized sparse operators.&lt;/p&gt;
&lt;h3 id="connections-to-other-architectures"&gt;Connections to other architectures&lt;a class="headerlink" href="#connections-to-other-architectures" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;BERT&lt;/strong&gt; masks input tokens and predicts the missing tokens. MAE transfers this broad idea to image patches, but uses a much higher masking ratio and predicts pixels.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;GPT&lt;/strong&gt; uses causal autoregressive prediction rather than BERT-style bidirectional masked-token reconstruction. It is an inspiration for scalable self-supervised pretraining, not a direct architectural analogue of MAE.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Denoising autoencoders&lt;/strong&gt; corrupt an input and reconstruct the original signal. MAE is a form of denoising autoencoding, although its sparse encoder and asymmetric decoder differ from classical designs.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SimCLR and MoCo&lt;/strong&gt; use contrastive objectives with negative examples. &lt;strong&gt;BYOL and DINO&lt;/strong&gt; avoid explicit negatives but still match representations across augmented views. MAE instead constructs its pretext task through random masking and pixel reconstruction.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;MAE relies less heavily on view augmentation than these representation-matching systems, but augmentation is not entirely absent from its default training recipe.&lt;/p&gt;
&lt;h3 id="practical-findings"&gt;Practical findings&lt;a class="headerlink" href="#practical-findings" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;h4 id="masking-ratio"&gt;Masking ratio&lt;a class="headerlink" href="#masking-ratio" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p align="center"&gt;&lt;img alt="Masking-ratio performance graph" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/maskratioperf.png"&gt;
Figure 3. ImageNet fine-tuning and linear-probe accuracy at different masking ratios.
&lt;br&gt;&lt;/p&gt;
&lt;p&gt;The best masking ratio depends on the evaluation protocol. Linear-probe accuracy peaks at 75% masking with 73.5%. Fine-tuning is less sensitive: ratios from roughly 40% to 80% all perform strongly, with the plotted maximum of 85.0% at 60%.&lt;/p&gt;
&lt;p&gt;The reconstructions below illustrate how outputs change when the same model, pretrained with 75% masking, is evaluated with 75%, 85%, and 95% masking.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="Reconstructions at different masking ratios" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/MaskPercentages.png"&gt;
Figure 4. Original image and reconstructions at mask ratios of 75%, 85%, and 95%.
&lt;br&gt;&lt;/p&gt;
&lt;h4 id="training-duration"&gt;Training duration&lt;a class="headerlink" href="#training-duration" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The reported accuracy continues to improve through the longest evaluated schedule of 1600 epochs.&lt;sup id="sf-Masked-Autoencoders-Are-Scalable-Vision-Learners-2-back"&gt;&lt;a href="#sf-Masked-Autoencoders-Are-Scalable-Vision-Learners-2" class="simple-footnote" title="Long ViT training schedules are discussed in How to Train Your ViT? Data, Augmentation, and Regularization in Vision Transformers. A related perspective on long training appears in Knowledge Distillation: A Good Teacher Is Patient and Consistent. These works provide context rather than direct evidence about MAE's mechanism."&gt;2&lt;/a&gt;&lt;/sup&gt;&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="Accuracy versus pretraining length" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/trainLength.png"&gt;
Figure 5. Fine-tuning and linear-probe accuracy improve with longer MAE pretraining in the evaluated 100-to-1600-epoch range.
&lt;br&gt;&lt;/p&gt;
&lt;p&gt;The result should not be extrapolated indefinitely: the experiment shows improvement up to 1600 epochs, not a general law that every longer schedule must improve accuracy.&lt;/p&gt;
&lt;h4 id="decoder-and-mask-token-ablations"&gt;Decoder and mask-token ablations&lt;a class="headerlink" href="#decoder-and-mask-token-ablations" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;A single Transformer-block decoder still reaches 84.8% after full fine-tuning. One block is the minimum needed for information to propagate between encoded visible tokens and mask tokens. A deeper decoder matters much more for linear probing: increasing decoder depth from one to eight blocks raises linear accuracy from 65.5% to 73.5%.&lt;/p&gt;
&lt;p align="center"&gt;&lt;a id="table-mae-ablation"&gt;&lt;/a&gt;
&lt;img alt="MAE decoder, target, augmentation, and masking ablations" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/advantages.png"&gt;
Table 1. MAE ablation experiments with ViT-L/16 on ImageNet-1K.
&lt;br&gt;&lt;/p&gt;
&lt;p&gt;Keeping mask tokens out of the encoder raises linear-probe accuracy from 59.6% to 73.5%, a 13.9 percentage-point improvement, while fine-tuning rises from 84.2% to 84.9%. It also reduces total training FLOPs by 3.3 times. The associated wall-clock speedup is 2.8 times for the default ViT-L decoder and ranges from 3.5 to 4.1 times in the listed ViT-H or shallow-decoder settings.&lt;/p&gt;
&lt;h4 id="comparison-with-other-self-supervised-methods"&gt;Comparison with other self-supervised methods&lt;a class="headerlink" href="#comparison-with-other-self-supervised-methods" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p align="center"&gt;&lt;a id="table-imagenet-comparison"&gt;&lt;/a&gt;
&lt;img alt="ImageNet comparison with other self-supervised methods" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/sslcomp.png"&gt;&lt;br&gt;
Table 2. ImageNet-1K end-to-end fine-tuning results reported in the MAE paper.
&lt;br&gt;&lt;/p&gt;
&lt;p&gt;For ViT-B, MAE reaches 83.6%, compared with 82.8% for DINO and 83.2% for both MoCo v3 and BEiT. For ViT-L, MAE reaches 85.9%, compared with 84.1% for MoCo v3 and 85.2% for BEiT; the table does not report DINO at this size. BEiT's discrete tokenizer was pretrained on 250 million DALL-E images, whereas MAE pretraining uses ImageNet-1K alone.&lt;/p&gt;
&lt;p&gt;These are end-to-end fine-tuning results. They do not establish that MAE features dominate every method under linear probing or every downstream protocol. Indeed, the paper emphasizes that MAE representations are less linearly separable than MoCo v3 representations.&lt;/p&gt;
&lt;p&gt;The 87.8% result uses ViT-H/14, 1600 epochs of ImageNet-1K pretraining, and fine-tuning at &lt;span class="math"&gt;\(448\times448\)&lt;/span&gt; resolution. MAE is also reported to be 3.5 times faster per pretraining epoch than BEiT.&lt;/p&gt;
&lt;h4 id="partial-fine-tuning"&gt;Partial fine-tuning&lt;a class="headerlink" href="#partial-fine-tuning" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;Partial fine-tuning updates only the final encoder blocks while freezing the earlier blocks. It provides a useful middle ground between linear probing and end-to-end fine-tuning.&lt;/p&gt;
&lt;p&gt;&lt;a id="figure-partial-finetuning"&gt;&lt;/a&gt;
&lt;img alt="MAE partial fine-tuning compared with MoCo v3" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/partialft.png"&gt;&lt;/p&gt;
&lt;p align="center"&gt;Figure 6. MAE and MoCo v3 accuracy as the number of fine-tuned ViT-L blocks increases.
&lt;br&gt;&lt;/p&gt;
&lt;p&gt;MAE is less accurate than MoCo v3 under linear probing, but becomes better when at least one Transformer block is fine-tuned. Fine-tuning one block raises MAE accuracy from 73.5% to 81.0%. The broader lesson is that linear separability is not the sole measure of representation quality; MAE learns strong features that benefit from a nonlinear adaptation.&lt;/p&gt;
&lt;h3 id="what-the-reconstructions-mean"&gt;What the reconstructions mean&lt;a class="headerlink" href="#what-the-reconstructions-mean" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The masks are random, not selected because the removed patches are semantically insignificant. In fact, the paper notes that random patches are unlikely to form complete semantic segments. Likewise, raw pixels are not semantic entities.&lt;/p&gt;
&lt;p&gt;Nevertheless, MAE can produce plausible object- and scene-level reconstructions, including outputs that differ from the exact ground truth. The authors interpret this behavior as evidence that a rich latent representation has emerged. This is a hypothesis supported by qualitative reconstructions and downstream transfer results, not proof that the model explicitly represents named concepts.&lt;/p&gt;
&lt;p&gt;The authors argue that MAE's scaling behavior may help move computer-vision pretraining from a predominantly supervised paradigm toward greater use of self-supervision.&lt;/p&gt;
&lt;h3 id="applications"&gt;Applications&lt;a class="headerlink" href="#applications" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;MAE is suited to visual representation pretraining followed by:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;image classification through linear probing or fine-tuning;&lt;/li&gt;
&lt;li&gt;object detection;&lt;/li&gt;
&lt;li&gt;instance segmentation;&lt;/li&gt;
&lt;li&gt;semantic segmentation; and&lt;/li&gt;
&lt;li&gt;transfer to fine-grained or scene-classification datasets.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="benchmark-results"&gt;Benchmark results&lt;a class="headerlink" href="#benchmark-results" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The paper evaluates ImageNet-1K classification and several transfer tasks. Comparisons should be interpreted within each table's architecture, pretraining data, and fine-tuning protocol.&lt;/p&gt;
&lt;h4 id="object-detection-and-instance-segmentation"&gt;Object detection and instance segmentation&lt;a class="headerlink" href="#object-detection-and-instance-segmentation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The authors fine-tune a ViT-based Mask R-CNN with a feature pyramid network on COCO and report box AP for detection and mask AP for instance segmentation. MAE beats supervised ImageNet-1K pretraining in every listed ViT-B and ViT-L configuration.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="MAE object detection and instance segmentation results" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/objdetec.png"&gt;&lt;br&gt;
Table 3. COCO object detection and instance segmentation results.
&lt;br&gt;&lt;/p&gt;
&lt;p&gt;For ViT-L, MAE and BEiT both reach 53.3 box AP, while MAE reaches 47.2 mask AP versus BEiT's 47.1. The pixel-based MAE is therefore better than or effectively on par with token-based BEiT here, while avoiding dVAE tokenizer pretraining.&lt;/p&gt;
&lt;h4 id="semantic-segmentation"&gt;Semantic segmentation&lt;a class="headerlink" href="#semantic-segmentation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The paper evaluates semantic segmentation on ADE20K using UperNet.&lt;/p&gt;
&lt;p align="center"&gt;&lt;img alt="MAE and BEiT semantic-segmentation results" src="https://mchromiak.github.io/articles/2021/Nov/14/Masked-Autoencoders-Are-Scalable-Vision-Learners/img/semsegm.png"&gt;&lt;br&gt;
Table 4. ADE20K semantic segmentation results in mean intersection over union.
&lt;br&gt;&lt;/p&gt;
&lt;p&gt;MAE reaches 48.1 mIoU with ViT-B and 53.6 with ViT-L. The ViT-L result is 3.7 points above supervised pretraining and 0.3 above BEiT.&lt;/p&gt;
&lt;h4 id="classification-transfer"&gt;Classification transfer&lt;a class="headerlink" href="#classification-transfer" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The paper additionally reports transfer to iNaturalist 2017, 2018, and 2019, plus Places205 and Places365. These experiments show strong scaling with larger ViT models. At the time of publication, several results exceeded systems pretrained on much larger external datasets, although such system-level comparisons do not isolate pretraining method as the only variable.&lt;/p&gt;
&lt;h3 id="further-research"&gt;Further research&lt;a class="headerlink" href="#further-research" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The paper already compares random, block-wise, and grid-wise masks and finds random sampling strongest. Natural extensions include:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;evaluating additional structured or content-aware masking strategies;&lt;/li&gt;
&lt;li&gt;varying the number of visible patches dynamically during pretraining; and&lt;/li&gt;
&lt;li&gt;studying whether mask-ratio schedules change the balance between reconstruction quality, efficiency, and transfer performance.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;These are proposed research directions, not claims evaluated in the MAE paper.&lt;/p&gt;
&lt;h3 id="useful-resources"&gt;Useful resources&lt;a class="headerlink" href="#useful-resources" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://openaccess.thecvf.com/content/CVPR2022/html/He_Masked_Autoencoders_Are_Scalable_Vision_Learners_CVPR_2022_paper.html"&gt;"Masked Autoencoders Are Scalable Vision Learners" - CVPR 2022 paper&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://github.com/facebookresearch/mae"&gt;Official MAE implementation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/2111.06377"&gt;"Masked Autoencoders Are Scalable Vision Learners" - arXiv&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/2106.08254"&gt;"BEiT: BERT Pre-Training of Image Transformers"&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/2006.07733"&gt;"Bootstrap Your Own Latent: A New Approach to Self-Supervised Learning"&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/2002.05709"&gt;"A Simple Framework for Contrastive Learning of Visual Representations"&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/1911.05722"&gt;"Momentum Contrast for Unsupervised Visual Representation Learning"&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;script type="text/javascript"&gt;if (!document.getElementById('mathjaxscript_pelican_#%@#$@#')) {
    var align = "center",
        indent = "0em",
        linebreak = "false";

    if (false) {
        align = (screen.width &lt; 768) ? "left" : align;
        indent = (screen.width &lt; 768) ? "0em" : indent;
        linebreak = (screen.width &lt; 768) ? 'true' : linebreak;
    }

    var mathjaxscript = document.createElement('script');
    mathjaxscript.id = 'mathjaxscript_pelican_#%@#$@#';
    mathjaxscript.type = 'text/javascript';
    mathjaxscript.src = 'https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.3/latest.js?config=TeX-AMS-MML_HTMLorMML';

    var configscript = document.createElement('script');
    configscript.type = 'text/x-mathjax-config';
    configscript[(window.opera ? "innerHTML" : "text")] =
        "MathJax.Hub.Config({" +
        "    config: ['MMLorHTML.js']," +
        "    TeX: { extensions: ['AMSmath.js','AMSsymbols.js','noErrors.js','noUndefined.js'], equationNumbers: { autoNumber: 'none' } }," +
        "    jax: ['input/TeX','input/MathML','output/HTML-CSS']," +
        "    extensions: ['tex2jax.js','mml2jax.js','MathMenu.js','MathZoom.js']," +
        "    displayAlign: '"+ align +"'," +
        "    displayIndent: '"+ indent +"'," +
        "    showMathMenu: true," +
        "    messageStyle: 'normal'," +
        "    tex2jax: { " +
        "        inlineMath: [ ['\\\\(','\\\\)'] ], " +
        "        displayMath: [ ['$$','$$'] ]," +
        "        processEscapes: true," +
        "        preview: 'TeX'," +
        "    }, " +
        "    'HTML-CSS': { " +
        "        availableFonts: ['STIX', 'TeX']," +
        "        preferredFont: 'STIX'," +
        "        styles: { '.MathJax_Display, .MathJax .mo, .MathJax .mi, .MathJax .mn': {color: 'inherit ! important'} }," +
        "        linebreaks: { automatic: "+ linebreak +", width: '90% container' }," +
        "    }, " +
        "}); " +
        "if ('default' !== 'default') {" +
            "MathJax.Hub.Register.StartupHook('HTML-CSS Jax Ready',function () {" +
                "var VARIANT = MathJax.OutputJax['HTML-CSS'].FONTDATA.VARIANT;" +
                "VARIANT['normal'].fonts.unshift('MathJax_default');" +
                "VARIANT['bold'].fonts.unshift('MathJax_default-bold');" +
                "VARIANT['italic'].fonts.unshift('MathJax_default-italic');" +
                "VARIANT['-tex-mathit'].fonts.unshift('MathJax_default-italic');" +
            "});" +
            "MathJax.Hub.Register.StartupHook('SVG Jax Ready',function () {" +
                "var VARIANT = MathJax.OutputJax.SVG.FONTDATA.VARIANT;" +
                "VARIANT['normal'].fonts.unshift('MathJax_default');" +
                "VARIANT['bold'].fonts.unshift('MathJax_default-bold');" +
                "VARIANT['italic'].fonts.unshift('MathJax_default-italic');" +
                "VARIANT['-tex-mathit'].fonts.unshift('MathJax_default-italic');" +
            "});" +
        "}";

    (document.body || document.getElementsByTagName('head')[0]).appendChild(configscript);
    (document.body || document.getElementsByTagName('head')[0]).appendChild(mathjaxscript);
}
&lt;/script&gt;&lt;ol class="simple-footnotes"&gt;&lt;li id="sf-Masked-Autoencoders-Are-Scalable-Vision-Learners-1"&gt;The first author and project lead of MAE, Kaiming He, was also the first author of &lt;a href="https://arxiv.org/abs/1512.03385"&gt;Deep Residual Learning for Image Recognition&lt;/a&gt; and &lt;a href="https://arxiv.org/abs/1911.05722"&gt;Momentum Contrast for Unsupervised Visual Representation Learning&lt;/a&gt;. &lt;a href="#sf-Masked-Autoencoders-Are-Scalable-Vision-Learners-1-back" class="simple-footnote-back"&gt;↩︎&lt;/a&gt;&lt;/li&gt;&lt;li id="sf-Masked-Autoencoders-Are-Scalable-Vision-Learners-2"&gt;Long ViT training schedules are discussed in &lt;a href="https://arxiv.org/abs/2106.10270"&gt;How to Train Your ViT? Data, Augmentation, and Regularization in Vision Transformers&lt;/a&gt;. A related perspective on long training appears in &lt;a href="https://arxiv.org/abs/2106.05237"&gt;Knowledge Distillation: A Good Teacher Is Patient and Consistent&lt;/a&gt;. These works provide context rather than direct evidence about MAE's mechanism. &lt;a href="#sf-Masked-Autoencoders-Are-Scalable-Vision-Learners-2-back" class="simple-footnote-back"&gt;↩︎&lt;/a&gt;&lt;/li&gt;&lt;/ol&gt;</content><category term="Computer Vision"/><category term="Representation Learning"/><category term="Self-Supervision (SSL)"/><category term="CV"/><category term="ViT"/><category term="BERT"/><category term="GPT"/><category term="Autoencoder"/><category term="Scalability"/></entry><entry><title>MLP-Mixer: MLP is all you need... again? ...</title><link href="https://mchromiak.github.io/articles/2021/May/05/MLP-Mixer/" rel="alternate"></link><published>2021-05-05T15:54:00+02:00</published><updated>2021-05-05T15:54:00+02:00</updated><author><name>Michał Chromiak</name></author><id>tag:mchromiak.github.io,2021-05-05:/articles/2021/May/05/MLP-Mixer/</id><summary type="html">&lt;p&gt;Let's try to answer the question: is it enough to have the FFN MLP, with matrix multiplication routines and scalar non-linearities to compete with modern architectures such as ViT or CNNs? No need for convolution, attention? It sounds that we have been here in the past. However, does it mean that the researchers are lost and go rounding in circles? It turns out that what has changes along the way is the increase in the scale of the resources and the data which originally helped ML and especially DL flourish past 5-7 years ago. We will discuss the paper which proves that MLP based solutions can replace CNN and attention based Transformers with comparable scores at image classification benchmarks and at pre-training/inference costs similar to SOTA models.&lt;/p&gt;</summary><content type="html">&lt;p&gt;&lt;img alt="MLP-Mixer general architecture" src="https://mchromiak.github.io/articles/2021/May/05/MLP-Mixer/img/Mixer.png"&gt;
&lt;br&gt;&lt;/p&gt;
&lt;h4 id=tldr&gt;TL:DR&lt;a class=headerlink href=#tldr title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;&lt;strong&gt;MLP is all you need to get adequately comparable results to SOTA CNNs and Transformer - while reaching linear complexity in number of input pixels. Conversely to CNNs, MLP is not localized as its filter spans entire spatial area&lt;/strong&gt;&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;The return to this idea deserves some deeper consideration. If you start thinking that researchers have already been here please read:&lt;/p&gt;
&lt;p&gt;(...) &lt;em&gt;As in the games, researchers always tried to make systems that worked the way the researchers thought their own minds worked---they tried to put that
knowledge in their systems---but it proved ultimately counterproductive, and a colossal waste of researcher's time&lt;/em&gt; (...)&lt;/p&gt;
&lt;p&gt;&lt;a href="http://www.incompleteideas.net/IncIdeas/BitterLesson.html"&gt;The Bitter Lesson form Rich Sutton, March 13. 2019&lt;/a&gt;&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h4 id=contribution-of-paper&gt;Contribution of paper:&lt;a class=headerlink href=#contribution-of-paper title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;Proves that MLP based architecture can be on pair with modern SOTA in terms of accuracy and computational resource trade-off for training and inference.&lt;/li&gt;
&lt;li&gt;Mixer has linear computation complexity in number of input pixels, in contrast to ViT and similarly to CNNs.&lt;/li&gt;
&lt;li&gt;Unlike ViT, no need for position embedding (token-mixing MLP is sensitive to the order of input tokens)&lt;/li&gt;
&lt;li&gt;Uses standard classification head with global average pooling followed by linear classifier.&lt;/li&gt;
&lt;li&gt;Mixer-MLP scales better than attention based counterparts for bigger training sets to an extend that it  is on pair with them&lt;sup id=sf-MLP-Mixer-1-back&gt;&lt;a href=#sf-MLP-Mixer-1 class=simple-footnote title=" One interesting question is what would happen with even bigger training sets than the ones used in experiments? Would Mixer supersede remaining SOTA architectures?"&gt;1&lt;/a&gt;&lt;/sup&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Raise questions on:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;How useful would it be to compare the features learned from CNN/Transformer solutions and those learned from such a simple architecture?&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;Consequently:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;What is the role of the inductive biases of such features and how they influence the generalization?&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=intro&gt;Intro&lt;a class=headerlink href=#intro title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;In this article we will investigate the paper:&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;"MLP-Mixer: An all-MLP Architecture for Vision" Ilya Tolstikhin et al. 2021&lt;/strong&gt;: &lt;a href="https://arxiv.org/abs/2105.01601"&gt;ArXiv - Submitted on 4 May 2021&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;The current, established computer vision architectures are based on CNNs and attention. The self-attention oriented modern Vision Transformer (ViT) models relies heavily on learning from raw data. The idea presented in the paper is simply to apply MLP repeatedly for spacial locations and feature channels.&lt;/p&gt;
&lt;h3 id=motivation&gt;Motivation&lt;a class=headerlink href=#motivation title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The Transformer/CNN trend in the area has dominated research in terms of SOTA results. Authors claim that the paper goal is to initiate discussion and opening questions on how the feature learned from both the MLP and present dominating approaches can be compared? It is also interesting how the induced biases contained within the features compares and influence the ability to generalize.&lt;/p&gt;
&lt;h3 id=the-processing-strategy-of-the-algorithm&gt;The processing strategy of the algorithm&lt;a class=headerlink href=#the-processing-strategy-of-the-algorithm title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Similarly to the  &lt;a href="https://arxiv.org/abs/2105.01601"&gt;&lt;em&gt;Vision Transformer&lt;/em&gt;&lt;/a&gt;. Each input image is divided into a grid of linearly projected, non-overlapping $16\times 16$ pixel patches (&lt;em&gt;tokens&lt;/em&gt;). Patches are put into one table of $patches\times channels$ as an input for the architecture. This is conversely to previous architectures, where patches would be unrolled into one, single vector of consecutive patches.&lt;/p&gt;
&lt;p&gt;The patches are basic parts that the architecture is working on as they are propagated through the network. The Mixer stacked layers are all of the same size. This is unlike CNNs where we shrink the resolution, but increase the channels.   &lt;/p&gt;
&lt;p align=center&gt;&lt;img alt="MLP-Mixer general architecture" src="https://mchromiak.github.io/articles/2021/May/05/MLP-Mixer/img/MLP-Mixer.png"&gt;
&lt;br&gt;
Figure 1. MLP-Mixer general architecture.&lt;/p&gt;
&lt;p&gt;At first every patch is feed through per-patch, fully connected layer, providing the latent vector representation (embeddings) per each patch. This is then passed to multiple &lt;em&gt;Mixer layers&lt;/em&gt; of the same size – each layer build out of two MLP blocks.&lt;/p&gt;
&lt;p&gt;There are two types of Mixer-MLP blocks that acts on rows and columns of the input table:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Token-Mixing&lt;/strong&gt; ; $Channels \times Patches$ (acts on columns) enables &lt;em&gt;communication across different spacial locations&lt;/em&gt; (tokens) of an image present in a single channel. A single column of the input table collects all tokens’ parts (across different patches) of a single channel.&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;&lt;strong&gt;Channel-Mixing&lt;/strong&gt; $Patches \times Channels$ (acts on rows); enables communication across different channels as their input is an individual token as a row from the input table.    &lt;/p&gt;
&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;MLP-Mixer use the idea of small kernel convolutions. The channel-mixing MLP_2 with $1\times1$ convolutions becomes effectively dense matrix multiplication applied independently to each spacial location. However, it does not allow aggregation of cross-spacial information. This motivates the token-mixing MLP_1 phase that includes the dense matrix multiplications applied to each feature/channel across all spatial locations.&lt;/p&gt;
&lt;h4 id=the-mlp-block-structure&gt;The MLP block structure&lt;a class=headerlink href=#the-mlp-block-structure title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The latent representation of patches form the fully-connected layer is first feed into the &lt;em&gt;token-mixer&lt;/em&gt; in a $channel x patch$ table. Whit that in mind, it takes all the patches (columns) per channel (row) and feeds it to the  &lt;em&gt;MLP1&lt;/em&gt; (two fully connected layers connected with GELU). Each MLP consists of two fully-connected layers and a non-linearity - GELU -  applied independently to each row of its input data tensor.&lt;/p&gt;
&lt;p&gt;The Mixer approach, is based on the idea that channels in individual patches mean similar things. It is because they come from same function of the per-patch fully-connected layer. This first layer put same object information into same channels for different patches. Thus, as they mean similar things, we will group them by channels, and aggregate over all patches within each channel. Therefore each channel have same information in form of feature.&lt;/p&gt;
&lt;p align=center&gt;&lt;img alt="MLP-Mixer layer details" src="https://mchromiak.github.io/articles/2021/May/05/MLP-Mixer/img/MLP-Mixer-layer.png"&gt;
&lt;br&gt;
Figure 2. Detailed architecture schema of MLP-Mixer layer.&lt;/p&gt;
&lt;p&gt;Each of the patches is feed through the same &lt;em&gt;MLP1&lt;/em&gt; with shared weights across all columns of the input table. Thus, we are dealing here with a dense matrix multiplication with a weight sharing across same channel of different patches&lt;sup id=sf-MLP-Mixer-2-back&gt;&lt;a href=#sf-MLP-Mixer-2 class=simple-footnote title="Tying parameters across channels is less common with contrast to previous solutions based on CNNs and self-attention."&gt;2&lt;/a&gt;&lt;/sup&gt;. In token-mixing, the &lt;em&gt;MLP_1&lt;/em&gt; share the same kernel (of full receptive field) for all of the channels. Therefore, if a channel reacts as a feature across the patches, it is easy to aggregate all the patches that include this feature.&lt;/p&gt;
&lt;p&gt;Second stage is to transpose back to $patches x channels$ and repeat same  computation in &lt;em&gt;MLP_2&lt;/em&gt; however, this time with weight sharing across all patches (rows of the input table). This kind of sharing provides positional invariance, which happens to also be part of CNNs. For each patch we apply computation across all of the channels (features) of that patch.&lt;/p&gt;
&lt;p&gt;Other components include: skip-connections, dropout, layer norm on the channels, and linear classifier head&lt;/p&gt;
&lt;h3 id=objective-or-goal-for-the-algorithm&gt;Objective or goal for the algorithm&lt;a class=headerlink href=#objective-or-goal-for-the-algorithm title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The general idea of the MLP-Mixer is to separate the &lt;em&gt;channel-mixing&lt;/em&gt; (per-location) operations and the &lt;em&gt;cross-location&lt;/em&gt; (token-mixing) operations. This is what distinguish it from the solutions described in following section.
This way if we aggregate among the same channels (token-mixing) then if a channel reacts across the patches we can aggregate all the patches that have that feature because the feature producing map was shared.&lt;/p&gt;
&lt;p&gt;Mixer computational complexity is linear in the number of input patches (input sequence length) in token-mixing, conversely to the previous ViT architecture - whose computational complexity is quadratic. Additionally, as the channel hidden width in channel-mixing, is also independent of the patch size, the overall complexity is &lt;strong&gt;linear&lt;/strong&gt; in the number of pixels. Which is also the case with CNNs.&lt;/p&gt;
&lt;h3 id=metaphors-or-analogies-to-other-architectures-describing-the-behavior-of-the-algorithm&gt;Metaphors, or analogies to other architectures describing the behavior of the algorithm&lt;a class=headerlink href=#metaphors-or-analogies-to-other-architectures-describing-the-behavior-of-the-algorithm title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The MLP-Mixer does not use the convolutions, nor self-attention - which are heavily used by the current successful architectures.&lt;/p&gt;
&lt;p&gt;Reference to remaining, modern vision architectures can be based on two techniques of layers that mix features at:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;one spatial location&lt;/li&gt;
&lt;li&gt;across different spatial locations,&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Both of those can be applied exclusively, or at once.&lt;/p&gt;
&lt;p&gt;In CNNs - deeper layers have larger receptive field – with $N \times N, N&amp;gt;1$ convolutions and pooling implement the second approach while with $1 \times 1$ would use first one.  &lt;br&gt;
The Mixer architecture is a very special case of CNN with $1 \times 1$ convolutions in channel-mixing, and for token-mixing it is a single-channel depth-wise convolution of a full receptive field with parameter sharing. Conversely, CNNs are not special cases of MLP-Mixer as the plain matrix multiplication of Mixer is less complex than costly convolutions specialized implementation.&lt;/p&gt;
&lt;p&gt;While referring to the attention-based architectures such as Vision Transformers, the self-attention enables both techniques while the MLP blocks are focusing only on the first one.&lt;/p&gt;
&lt;p&gt;The Mixer architecture embrace both techniques (using MLPs), but separates them clearly: the first, per-location operations is implemented in &lt;em&gt;channel-mixing&lt;/em&gt;, and the cross-location operations are implemented with the &lt;em&gt;token-mixing&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;Similarly to Transformers, each layer in Mixer (except for the initial patch projection layer) takes an input of the same (aka &lt;em&gt;isotropic&lt;/em&gt;) size. This is in contrast to typical CNN that have a pyramidal structure: deeper layers have a lower resolution input, but more channels&lt;sup id=sf-MLP-Mixer-3-back&gt;&lt;a href=#sf-MLP-Mixer-3 class=simple-footnote title="One can also refer to one of the non-typical designs, of other combinations such as isotropic ResNets and pyramidal ViTs."&gt;3&lt;/a&gt;&lt;/sup&gt;.&lt;/p&gt;
&lt;h3 id=discussion-is-mlp-mixer-a-simple-a-cnn-only-with-the-convolution-weights-being-decided-by-attention&gt;Discussion:  Is MLP-Mixer a simple a CNN only with the convolution weights being decided by attention?&lt;a class=headerlink href=#discussion-is-mlp-mixer-a-simple-a-cnn-only-with-the-convolution-weights-being-decided-by-attention title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;One can argue that this is simply a convolution in form of the "image patches" and that due to that the only thing that is proven here is that: the larger the receptive field is - the better results we can get. In which case this would be simply a $16\times16$ non-overlapping filters (stride 16). Moreover, then the architecture would be simply an MLP over kernel&lt;/p&gt;
&lt;p&gt;However, if we take a closer look, the CNN is localized, so as such the filter is not covering the whole spacial area, with the Mixer it does.&lt;/p&gt;
&lt;h3 id=heuristics-or-rules-of-thumb&gt;Heuristics or rules of thumb&lt;a class=headerlink href=#heuristics-or-rules-of-thumb title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Authors have commenced series of tests with couple of parameters. The patch resolution e.g. $16 \times 16$. The scale expressed in number of mixer layers: &lt;strong&gt;S&lt;/strong&gt;mall: 8, &lt;strong&gt;B&lt;/strong&gt;ig: 16, &lt;strong&gt;L&lt;/strong&gt;arge: 24 and &lt;strong&gt;H&lt;/strong&gt;uge: 32 following the &lt;a href="https://arxiv.org/abs/2105.01601"&gt;&lt;em&gt;Vision Transformer (ViT)&lt;/em&gt;&lt;/a&gt; paper approach&lt;sup id=sf-MLP-Mixer-4-back&gt;&lt;a href=#sf-MLP-Mixer-4 class=simple-footnote title="One of the authors has actually also coauthored that ViT paper."&gt;4&lt;/a&gt;&lt;/sup&gt;.      &lt;/p&gt;
&lt;p&gt;The results of the experiments shows that the MLP-Mixer is comparable with current SOTA however, is not as good. Therefore, authors have found a metric that distinguish the Mixer architecture. It is the &lt;em&gt;Throughput&lt;/em&gt; (column 5 in the below figure) defined as number of images per second per computation core.&lt;/p&gt;
&lt;p align=center&gt;&lt;img alt="MLP-Mixer layer details" src="https://mchromiak.github.io/articles/2021/May/05/MLP-Mixer/img/Mixer-Throughput.png"&gt;
&lt;br&gt;
Figure 3. Transfer performance, inference throughput, and training cost.Sorted by inference throughput.&lt;/p&gt;
&lt;p&gt;Additionally, authors have found out that on a very particular task of training linear 5-shoot classifier on frozen representation of what the model outcomes evaluated on Top-1 accuracy.&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;Note: Top-1 accuracy is the conventional accuracy, model prediction (the one with the highest
probability) must be exactly the expected answer. It measures the proportion of examples for
which the predicted label matches the single target label.&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;This has been evaluated, and discussed in context of the role of model scale.&lt;/p&gt;
&lt;h4 id=scalability-mixer-is-catching-up&gt;Scalability - Mixer is catching up&lt;a class=headerlink href=#scalability-mixer-is-catching-up title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;With training set size increase the authors state that the scalability of Mixer is much more favorably comparing to big transfer (BiT) that plateaus and is on pair with ViT. Moreover, in all cases the differences in the scalability performance of Mixer is getting very close, if not same, as remaining models.&lt;/p&gt;
&lt;p align=center&gt;&lt;img alt="MLP-Mixer layer details" src="https://mchromiak.github.io/articles/2021/May/05/MLP-Mixer/img/Top1-scalability.png"&gt;
&lt;br&gt;
Figure 4. Transfer performance, inference throughput, and training cost.Sorted by inference throughput.&lt;/p&gt;
&lt;p&gt;This is the most significant result, but authors compare also some more tasks, for details please refer to the paper.
The conclusion from the results however might be, that the smaller the training set, the worse the Mixer performs in terms of pre-training, and the bigger training set, the gap between the Mixer and remaining architectures (ViT, BiT) shrinks.&lt;/p&gt;
&lt;h4 id=weight-visualization&gt;Weight visualization&lt;a class=headerlink href=#weight-visualization title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The weight visualization analysis confirm the general assumption on how we observe the neural network work. In the image we see that lower layers learn first the most general features (left), then go to bit larger ones (middle) and the detailed (right).&lt;/p&gt;
&lt;p align=center&gt;&lt;img alt="MLP-Mixer weight visualization" src="https://mchromiak.github.io/articles/2021/May/05/MLP-Mixer/img/MLP-Mixer-Weights.png"&gt;
&lt;br&gt;
Figure 5. MLP-Mixer input weights to hidden units visualization in token-mixing MLPs of Mixer-B/16 model trained on JFT-300M proprietary Google dataset used for training image classification models.&lt;/p&gt;
&lt;p&gt;Interestingly the weights visualization differs based on the type of the training set that authors have used:&lt;/p&gt;
&lt;h3 id=what-classes-of-problem-is-the-algorithm-well-suited&gt;What classes of problem is the algorithm well suited?&lt;a class=headerlink href=#what-classes-of-problem-is-the-algorithm-well-suited title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The architecture seems useful especially for big scale training sets of vision tasks - as tested according to the description in next section. However, it might as well be applied to some NLP tasks however, with adequately prepared text “patches”.  &lt;/p&gt;
&lt;h3 id=common-benchmark-or-example-datasets-used-to-demonstrate-the-algorithm&gt;Common benchmark or example datasets used to demonstrate the algorithm&lt;a class=headerlink href=#common-benchmark-or-example-datasets-used-to-demonstrate-the-algorithm title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Comparing to CNN or attention based is claimed to be on pair with the existing SOTA methods in terms of the trade-off between accuracy and computational resources required for training and inference.
Authors have tested downstream tasks such as:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;ILSVRC2012 “ImageNet” (1.3M training examples, 1k classes) with the original validation labels and cleaned-up ReaL labels,&lt;/li&gt;
&lt;li&gt;CIFAR-10/100 (50k examples, 10/100 classes),&lt;/li&gt;
&lt;li&gt;Oxford-IIIT Pets (3.7k examples, 36 classes), and&lt;/li&gt;
&lt;li&gt;Oxford Flowers-102 (2k examples, 102 classes) and also evaluate on the&lt;/li&gt;
&lt;li&gt;Visual Task Adaptation Benchmark (VTAB-1k), which consists of 19 diverse datasets, each with 1k training examples.&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id=footnotes&gt;Footnotes:&lt;a class=headerlink href=#footnotes title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;&lt;ol class=simple-footnotes&gt;&lt;li id=sf-MLP-Mixer-1&gt; One interesting question is what would happen with even bigger training sets than the ones used in experiments? Would Mixer supersede remaining SOTA architectures? &lt;a href=#sf-MLP-Mixer-1-back class=simple-footnote-back&gt;↩&lt;/a&gt;&lt;/li&gt;&lt;li id=sf-MLP-Mixer-2&gt;Tying parameters across channels is less common with contrast to previous solutions based on CNNs and self-attention. &lt;a href=#sf-MLP-Mixer-2-back class=simple-footnote-back&gt;↩&lt;/a&gt;&lt;/li&gt;&lt;li id=sf-MLP-Mixer-3&gt;One can also refer to one of the non-typical designs, of other combinations such as &lt;a href="https://arxiv.org/abs/1909.03205"&gt;isotropic ResNets&lt;/a&gt; and &lt;a href="https://arxiv.org/abs/2102.12122"&gt;pyramidal ViTs&lt;/a&gt;. &lt;a href=#sf-MLP-Mixer-3-back class=simple-footnote-back&gt;↩&lt;/a&gt;&lt;/li&gt;&lt;li id=sf-MLP-Mixer-4&gt;One of the authors has actually also coauthored that ViT paper. &lt;a href=#sf-MLP-Mixer-4-back class=simple-footnote-back&gt;↩&lt;/a&gt;&lt;/li&gt;&lt;/ol&gt;</content><category term="Computer Vision"></category><category term="MLP"></category><category term="Transformer"></category><category term="CV"></category><category term="ViT"></category></entry><entry><title>DINO: Self-supervised ViTs learn strong features and semantic structure</title><link href="https://mchromiak.github.io/articles/2021/May/03/DINO-Emerging-Properties-in-Self-Supervised-Vision-Transformers/" rel="alternate"/><published>2021-05-03T17:44:00+02:00</published><updated>2021-06-14T17:44:00+02:00</updated><author><name>Michał Chromiak</name></author><id>tag:mchromiak.github.io,2021-05-03:/articles/2021/May/03/DINO-Emerging-Properties-in-Self-Supervised-Vision-Transformers/</id><summary type="html">&lt;p&gt;Self-distillation with no labels (DINO) trains a student network to match an exponential-moving-average teacher across differently augmented views of the same image. Applied to Vision Transformers, it produces features that perform strongly with simple linear and k-nearest-neighbor classifiers. Its final-layer self-attention also reveals object boundaries without segmentation labels, although DINO is not itself a segmentation model.&lt;/p&gt;</summary><content type="html">&lt;h4 id="the-dino-paper-explained"&gt;The DINO paper explained&lt;a class="headerlink" href="#the-dino-paper-explained" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;This article reviews &lt;strong&gt;&lt;a href="https://openaccess.thecvf.com/content/ICCV2021/html/Caron_Emerging_Properties_in_Self-Supervised_Vision_Transformers_ICCV_2021_paper.html"&gt;Emerging Properties in Self-Supervised Vision Transformers&lt;/a&gt;&lt;/strong&gt; by Caron et al., published at ICCV 2021.&lt;/p&gt;
&lt;p&gt;DINO stands for self-&lt;strong&gt;DI&lt;/strong&gt;stillation with &lt;strong&gt;NO&lt;/strong&gt; labels. It is a self-supervised representation-learning method: it learns from images without human-provided class labels. A student network is optimized by gradient descent to match a teacher network's output for another view of the same image. The teacher has the same architecture but is not trained by backpropagation; its weights are an exponential moving average (EMA) of the student's weights.&lt;/p&gt;
&lt;p&gt;The method works with both convolutional networks and Vision Transformers (ViTs), but the paper's most interesting findings concern ViTs. DINO-trained ViTs provide strong image-level features and final-layer self-attention maps that often align with foreground objects and their boundaries.&lt;/p&gt;
&lt;h4 id="tldr"&gt;TL;DR&lt;a class="headerlink" href="#tldr" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;ul&gt;
&lt;li&gt;DINO is a non-contrastive self-supervised method inspired by self-distillation and momentum-teacher methods such as BYOL and Mean Teacher. It is not simply a successor to SimCLR, MoCo, or BYOL.&lt;/li&gt;
&lt;li&gt;The student and teacher use the same architecture but have different parameters.&lt;/li&gt;
&lt;li&gt;The student processes all augmented views; the teacher processes only the two large global crops.&lt;/li&gt;
&lt;li&gt;Training minimizes cross-entropy between the teacher's probability distribution for one view and the student's distribution for another view.&lt;/li&gt;
&lt;li&gt;The teacher is updated with an EMA of the student and receives no gradients.&lt;/li&gt;
&lt;li&gt;Centering and sharpening act in opposite directions and help prevent representation collapse in the presence of the momentum teacher.&lt;/li&gt;
&lt;li&gt;DINO does not require negative pairs, a contrastive loss, a prediction head, a memory queue, or batch normalization when used with ViT.&lt;/li&gt;
&lt;li&gt;A frozen DINO ViT-S/8 reaches &lt;strong&gt;78.3% ImageNet top-1 accuracy with weighted k-NN&lt;/strong&gt;. This is not zero-shot classification: the k-NN classifier uses labeled ImageNet training examples.&lt;/li&gt;
&lt;li&gt;DINO ViT-B/8 reaches &lt;strong&gt;80.1% ImageNet top-1 accuracy in linear evaluation&lt;/strong&gt;.&lt;/li&gt;
&lt;li&gt;Smaller ViT patches improve the reported accuracy without increasing parameter count, but they substantially reduce throughput and increase compute and memory requirements.&lt;/li&gt;
&lt;li&gt;ViT attention maps provide evidence of emergent object localization. They are not explicit semantic segmentation predictions and do not assign semantic classes to pixels.&lt;/li&gt;
&lt;/ul&gt;
&lt;p align="center"&gt;&lt;a name="fig:dino_segmentation"&gt;&lt;/a&gt;
&lt;img alt="Final-layer self-attention from a DINO ViT with 8 x 8 patches" src="https://mchromiak.github.io/articles/2021/May/03/DINO-Emerging-Properties-in-Self-Supervised-Vision-Transformers/img/dino-intro.png"&gt;
Figure 1. Selected attention heads often focus on foreground objects and align with object boundaries. These are attention maps, not semantic segmentation masks. (&lt;a href="https://github.com/facebookresearch/dino"&gt;Source&lt;/a&gt;)&lt;/p&gt;
&lt;h3 id="what-problem-does-dino-address"&gt;What problem does DINO address?&lt;a class="headerlink" href="#what-problem-does-dino-address" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;Supervised image classification compresses the training signal for an image into one label from a fixed vocabulary. Self-supervised learning instead constructs a learning objective from the data itself. The hope is that a model trained without class labels will retain visual information useful across many downstream tasks.&lt;/p&gt;
&lt;p&gt;The paper asks what happens when modern self-supervised methods are applied to ViTs. Its central contribution is therefore not a claim that DINO universally replaces supervised ViT training. It is an empirical study showing that:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;self-supervised ViTs can learn image representations that transfer well;&lt;/li&gt;
&lt;li&gt;their frozen features work unusually well with a simple k-NN classifier; and&lt;/li&gt;
&lt;li&gt;their final-layer class-token attention often exposes object layout and boundaries.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The authors also simplify the training recipe. DINO combines a momentum teacher, multi-crop augmentation, cross-view prediction, centering, and sharpening. Unlike many contrastive methods, it does not compare an image against explicit negative examples.&lt;/p&gt;
&lt;h3 id="method"&gt;Method&lt;a class="headerlink" href="#method" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;h4 id="multiple-views-of-one-image"&gt;Multiple views of one image&lt;a class="headerlink" href="#multiple-views-of-one-image" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;For each training image, DINO creates a set of augmented views:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;two &lt;strong&gt;global crops&lt;/strong&gt;, typically at resolution &lt;span class="math"&gt;\(224 \times 224\)&lt;/span&gt;; and&lt;/li&gt;
&lt;li&gt;several &lt;strong&gt;local crops&lt;/strong&gt;, typically at resolution &lt;span class="math"&gt;\(96 \times 96\)&lt;/span&gt;.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The original implementation uses eight local crops by default. Color jitter, grayscale conversion, Gaussian blur, solarization, and horizontal flipping further alter the views.&lt;/p&gt;
&lt;p&gt;This is not a reconstruction objective. Neither network is asked to reproduce the original pixels. Instead, the student must produce a similar high-level output for different crops of the same source image, including local crops that contain only part of the scene.&lt;/p&gt;
&lt;h4 id="student-and-teacher"&gt;Student and teacher&lt;a class="headerlink" href="#student-and-teacher" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;Let &lt;span class="math"&gt;\(g_{\theta_s}\)&lt;/span&gt; be the student and &lt;span class="math"&gt;\(g_{\theta_t}\)&lt;/span&gt; the teacher. They have the same backbone and projection-head architecture, but their parameters differ.&lt;/p&gt;
&lt;p&gt;The student receives every global and local view. The teacher receives only the two global views. For an input view &lt;span class="math"&gt;\(x\)&lt;/span&gt;, the networks produce probability distributions over &lt;span class="math"&gt;\(K\)&lt;/span&gt; learned output dimensions:&lt;/p&gt;
&lt;div class="math"&gt;$$
P_s(x)^{(i)} =
\frac{\exp(g_{\theta_s}(x)^{(i)} / \tau_s)}
{\sum_{k=1}^{K} \exp(g_{\theta_s}(x)^{(k)} / \tau_s)}
$$&lt;/div&gt;
&lt;p&gt;and&lt;/p&gt;
&lt;div class="math"&gt;$$
P_t(x)^{(i)} =
\frac{\exp((g_{\theta_t}(x)^{(i)} - c^{(i)}) / \tau_t)}
{\sum_{k=1}^{K} \exp((g_{\theta_t}(x)^{(k)} - c^{(k)}) / \tau_t)}.
$$&lt;/div&gt;
&lt;p&gt;Here, &lt;span class="math"&gt;\(c\)&lt;/span&gt; is the teacher-output center, while &lt;span class="math"&gt;\(\tau_s\)&lt;/span&gt; and &lt;span class="math"&gt;\(\tau_t\)&lt;/span&gt; are the student and teacher temperatures. These &lt;span class="math"&gt;\(K\)&lt;/span&gt; dimensions are learned targets, not human-defined classes.&lt;/p&gt;
&lt;p&gt;For each teacher global view &lt;span class="math"&gt;\(x\)&lt;/span&gt;, the student is trained on every other view &lt;span class="math"&gt;\(x'\)&lt;/span&gt; of the same image:&lt;/p&gt;
&lt;div class="math"&gt;$$
L =
\sum_{x \in \{x_1^g, x_2^g\}}
\sum_{\substack{x' \in V \\ x' \ne x}}
H(P_t(x), P_s(x')),
$$&lt;/div&gt;
&lt;p&gt;where &lt;span class="math"&gt;\(H(a,b)=-\sum_i a^{(i)}\log b^{(i)}\)&lt;/span&gt; is cross-entropy and &lt;span class="math"&gt;\(V\)&lt;/span&gt; contains all crops. The identical student-teacher view pair is excluded. Gradients flow only through the student.&lt;/p&gt;
&lt;h4 id="momentum-teacher"&gt;Momentum teacher&lt;a class="headerlink" href="#momentum-teacher" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;After each student update, the teacher parameters are updated as:&lt;/p&gt;
&lt;div class="math"&gt;$$
\theta_t \leftarrow \lambda \theta_t + (1-\lambda)\theta_s.
$$&lt;/div&gt;
&lt;p&gt;The momentum coefficient &lt;span class="math"&gt;\(\lambda\)&lt;/span&gt; follows a cosine schedule from 0.996 toward 1. This slowly changing teacher provides more stable targets than using the current student directly. In the paper's ViT-S/16 ablation, removing the momentum teacher causes collapse and yields only 0.1% k-NN and linear accuracy.&lt;/p&gt;
&lt;h4 id="projection-head-and-downstream-features"&gt;Projection head and downstream features&lt;a class="headerlink" href="#projection-head-and-downstream-features" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The projection head is a three-layer multilayer perceptron with a 2048-dimensional hidden layer, followed by &lt;span class="math"&gt;\(\ell_2\)&lt;/span&gt; normalization and a weight-normalized fully connected layer. The self-supervised loss is applied to this head.&lt;/p&gt;
&lt;p&gt;For downstream evaluation, the authors discard the projection head and use the frozen backbone representation. For ViTs, this representation is based on the class token.&lt;/p&gt;
&lt;script src="https://vjs.zencdn.net/7.11.4/video.min.js"&gt;&lt;/script&gt;
&lt;!-- webm is substantially smaller than the equivalent gif --&gt;
&lt;video id="my-player" class="video-js vjs-theme-sea vjs-big-play-centered" controls preload="metadata" autoplay loop muted playsinline style="max-width: 100%; height: auto;"&gt;
 &lt;source src="../img/dino.webm" type="video/webm"&gt;
 &lt;p class="vjs-no-js"&gt;
   To view this video, enable JavaScript or use a browser that supports
   &lt;a href="https://videojs.com/html5-video-support/" target="_blank" rel="noopener"&gt;HTML5 video&lt;/a&gt;.
 &lt;/p&gt;
&lt;/video&gt;

&lt;p align="center"&gt;Figure 2. DINO training overview. The student sees global and local crops; the EMA teacher sees global crops. (&lt;a href="https://github.com/facebookresearch/dino"&gt;Source&lt;/a&gt;)&lt;/p&gt;
&lt;h3 id="avoiding-collapse"&gt;Avoiding collapse&lt;a class="headerlink" href="#avoiding-collapse" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;h4 id="what-is-representation-collapse"&gt;What is representation collapse?&lt;a class="headerlink" href="#what-is-representation-collapse" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;A collapsed network maps many or all inputs to the same output. Such a constant representation can make two augmented views agree perfectly while carrying no useful information about the image.&lt;/p&gt;
&lt;p&gt;Contrastive approaches discourage this solution using negative examples. DINO instead combines a momentum teacher with two operations on the teacher output:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Centering&lt;/strong&gt; subtracts an EMA of the batch-average teacher logits. It prevents one output dimension from dominating, but by itself tends toward a uniform output distribution.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Sharpening&lt;/strong&gt; uses a low teacher temperature to make the teacher distribution more selective. It counters uniform collapse, but by itself can encourage domination by a small number of dimensions.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;These effects balance each other. The paper is careful about the scope of this conclusion: centering and sharpening avoid collapse &lt;strong&gt;when used with the momentum teacher&lt;/strong&gt;. They are not demonstrated as a universal collapse-prevention recipe for arbitrary architectures.&lt;/p&gt;
&lt;p&gt;The default student temperature is 0.1. For the teacher, the temperature is warmed from 0.04 to 0.07 during the first 30 epochs. The center is also updated by an EMA of teacher outputs.&lt;/p&gt;
&lt;h3 id="what-emerges-in-a-dino-trained-vit"&gt;What emerges in a DINO-trained ViT?&lt;a class="headerlink" href="#what-emerges-in-a-dino-trained-vit" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;h4 id="strong-frozen-features"&gt;Strong frozen features&lt;a class="headerlink" href="#strong-frozen-features" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The authors evaluate frozen representations in two main ways:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Linear evaluation:&lt;/strong&gt; train a supervised linear classifier on top of the frozen features.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Weighted k-NN evaluation:&lt;/strong&gt; store frozen features from the labeled ImageNet training set and classify each validation image by a weighted vote among its nearest stored features.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;The second protocol is simple, but it is still supervised at evaluation time because it uses labels from the ImageNet training set. Calling it zero-shot would be incorrect.&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Backbone&lt;/th&gt;
&lt;th style="text-align: right;"&gt;Patch size&lt;/th&gt;
&lt;th style="text-align: right;"&gt;k-NN top-1&lt;/th&gt;
&lt;th style="text-align: right;"&gt;Linear top-1&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;ResNet-50&lt;/td&gt;
&lt;td style="text-align: right;"&gt;-&lt;/td&gt;
&lt;td style="text-align: right;"&gt;67.5%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;75.3%&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ViT-S&lt;/td&gt;
&lt;td style="text-align: right;"&gt;&lt;span class="math"&gt;\(16 \times 16\)&lt;/span&gt;&lt;/td&gt;
&lt;td style="text-align: right;"&gt;74.5%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;77.0%&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ViT-S&lt;/td&gt;
&lt;td style="text-align: right;"&gt;&lt;span class="math"&gt;\(8 \times 8\)&lt;/span&gt;&lt;/td&gt;
&lt;td style="text-align: right;"&gt;&lt;strong&gt;78.3%&lt;/strong&gt;&lt;/td&gt;
&lt;td style="text-align: right;"&gt;79.7%&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ViT-B&lt;/td&gt;
&lt;td style="text-align: right;"&gt;&lt;span class="math"&gt;\(16 \times 16\)&lt;/span&gt;&lt;/td&gt;
&lt;td style="text-align: right;"&gt;76.1%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;78.2%&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;ViT-B&lt;/td&gt;
&lt;td style="text-align: right;"&gt;&lt;span class="math"&gt;\(8 \times 8\)&lt;/span&gt;&lt;/td&gt;
&lt;td style="text-align: right;"&gt;77.4%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;&lt;strong&gt;80.1%&lt;/strong&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The paper generally uses &lt;span class="math"&gt;\(k=20\)&lt;/span&gt; for weighted k-NN. On the same ViT-S/16 architecture, DINO reaches 77.0% linear accuracy, 3.5 percentage points above the reported SwAV result. Its 74.5% k-NN accuracy is 7.9 points above the best listed BYOL result.&lt;/p&gt;
&lt;h4 id="smaller-patches-better-spatial-detail-higher-cost"&gt;Smaller patches: better spatial detail, higher cost&lt;a class="headerlink" href="#smaller-patches-better-spatial-detail-higher-cost" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;Reducing the patch size gives the transformer more spatial tokens and improves the reported representation quality. It does not add learned parameters, but self-attention becomes more expensive as the number of tokens grows.&lt;/p&gt;
&lt;p&gt;The paper reports throughput of roughly 1007 images/s for ViT-S/16, 180 images/s for ViT-S/8, and 44 images/s for ViT-S/5 under its measurement setup. Smaller patches therefore &lt;strong&gt;increase&lt;/strong&gt;, rather than reduce, running time and memory pressure.&lt;/p&gt;
&lt;h4 id="emergent-object-localization"&gt;Emergent object localization&lt;a class="headerlink" href="#emergent-object-localization" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;In a ViT, the final class token attends to patch tokens. DINO's final-layer attention heads often focus on coherent foreground regions. To quantify this, the authors threshold attention maps to retain 60% of their mass and compare them with object masks from PASCAL VOC 2012.&lt;/p&gt;
&lt;p&gt;DINO ViT-S/8 reaches 44.7% Jaccard similarity, compared with 23.7% for supervised ViT-S/8 and 21.8% for a randomly initialized model. This is evidence that object layout emerges in the representation without segmentation labels.&lt;/p&gt;
&lt;p&gt;There are important limits to the interpretation:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Attention maps are not semantic segmentation outputs.&lt;/li&gt;
&lt;li&gt;They do not assign a category to every pixel.&lt;/li&gt;
&lt;li&gt;Different attention heads may focus on different objects or parts.&lt;/li&gt;
&lt;li&gt;The evaluation measures overlap with foreground masks after thresholding; it does not establish a complete segmentation system.&lt;/li&gt;
&lt;/ul&gt;
&lt;h4 id="video-object-segmentation"&gt;Video object segmentation&lt;a class="headerlink" href="#video-object-segmentation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;The paper also evaluates frozen patch features on DAVIS 2017. Labels are propagated between frames using nearest-neighbor matching; the backbone itself is not fine-tuned for video segmentation. DINO ViT-S/8 obtains 69.9 mean &lt;span class="math"&gt;\(J\&amp;amp;F\)&lt;/span&gt;, compared with 66.0 for supervised ViT-S/8. DINO ViT-B/8 reaches 71.4.&lt;/p&gt;
&lt;p&gt;This experiment shows that local patch features encode useful correspondence. It does not mean that raw attention maps alone solve video instance segmentation.&lt;/p&gt;
&lt;h3 id="ablation-study"&gt;Ablation study&lt;a class="headerlink" href="#ablation-study" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The ViT-S/16 ablation after 300 epochs clarifies which parts of DINO matter:&lt;/p&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;Configuration&lt;/th&gt;
&lt;th style="text-align: right;"&gt;k-NN top-1&lt;/th&gt;
&lt;th style="text-align: right;"&gt;Linear top-1&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;Default DINO&lt;/td&gt;
&lt;td style="text-align: right;"&gt;72.8%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;76.1%&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Without momentum teacher&lt;/td&gt;
&lt;td style="text-align: right;"&gt;0.1%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;0.1%&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;Without multi-crop&lt;/td&gt;
&lt;td style="text-align: right;"&gt;67.9%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;72.5%&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;MSE instead of cross-entropy&lt;/td&gt;
&lt;td style="text-align: right;"&gt;52.6%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;62.4%&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;With an added predictor&lt;/td&gt;
&lt;td style="text-align: right;"&gt;71.8%&lt;/td&gt;
&lt;td style="text-align: right;"&gt;75.6%&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;The momentum teacher is essential in this setup, and multi-crop training provides a large improvement. Cross-entropy works substantially better than mean squared error. Unlike BYOL, DINO does not benefit from a separate predictor in this experiment.&lt;/p&gt;
&lt;h3 id="what-dino-does-not-establish"&gt;What DINO does not establish&lt;a class="headerlink" href="#what-dino-does-not-establish" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;The paper provides strong evidence for its tested architectures and benchmarks, but some broader conclusions would go beyond the experiments:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;DINO is not a universally superior replacement for supervised learning.&lt;/li&gt;
&lt;li&gt;Its transfer results are strong but not uniformly better on every dataset. For example, supervised ViT-B/16 slightly exceeds DINO on iNaturalist 2018 in the paper's transfer table.&lt;/li&gt;
&lt;li&gt;The learned output dimensions are not automatically named semantic categories.&lt;/li&gt;
&lt;li&gt;k-NN evaluation is not zero-shot learning.&lt;/li&gt;
&lt;li&gt;Attention visualization is evidence of localization, not a production-ready segmentation method.&lt;/li&gt;
&lt;li&gt;The method still requires substantial compute, careful augmentation, and large-scale image data.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="applications"&gt;Applications&lt;a class="headerlink" href="#applications" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;DINO representations are useful when labels are scarce or when a reusable visual encoder is needed. The paper demonstrates or motivates:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;image classification with linear probing, k-NN, or fine-tuning;&lt;/li&gt;
&lt;li&gt;image retrieval and copy detection;&lt;/li&gt;
&lt;li&gt;transfer to classification datasets;&lt;/li&gt;
&lt;li&gt;object discovery through attention analysis; and&lt;/li&gt;
&lt;li&gt;video object segmentation through frozen-feature correspondence.&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="relationship-to-other-methods"&gt;Relationship to other methods&lt;a class="headerlink" href="#relationship-to-other-methods" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;DINO belongs to a family of methods that learn by matching representations across image views.&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;SimCLR&lt;/strong&gt; uses a contrastive objective with in-batch negatives.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;MoCo&lt;/strong&gt; uses a momentum encoder and a queue of negative examples.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;BYOL&lt;/strong&gt; matches an online network to a momentum target network without negatives and uses a predictor.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SwAV&lt;/strong&gt; predicts cluster assignments between views and uses online clustering.&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;DINO&lt;/strong&gt; matches centered and sharpened teacher distributions using cross-entropy, a momentum teacher, and multi-crop views, without negatives or a predictor.&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;DINO can train ResNet-50 as well as ViTs. The paper's distinctive observations, however, are the strong k-NN behavior and emergent spatial structure of self-supervised ViT features.&lt;/p&gt;
&lt;h3 id="useful-resources"&gt;Useful resources&lt;a class="headerlink" href="#useful-resources" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;&lt;a href="https://openaccess.thecvf.com/content/ICCV2021/html/Caron_Emerging_Properties_in_Self-Supervised_Vision_Transformers_ICCV_2021_paper.html"&gt;ICCV 2021 paper&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/2104.14294"&gt;DINO on arXiv&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://github.com/facebookresearch/dino"&gt;Official PyTorch implementation&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://ai.meta.com/blog/dino-paws-computer-vision-with-self-supervised-transformers-and-10x-more-efficient-training/"&gt;DINO and PAWS overview&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/2006.07733"&gt;BYOL: Bootstrap Your Own Latent&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/1703.01780"&gt;Mean Teacher&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://arxiv.org/abs/1911.05722"&gt;MoCo: Momentum Contrast&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://mchromiak.github.io/articles/2023/Apr/18/DINOv2-Learning-Robust-Visual-Features-without-Supervision/"&gt;DINOv2 explained&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="terminology"&gt;Terminology&lt;a class="headerlink" href="#terminology" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h3&gt;
&lt;h4 id="knowledge-distillation"&gt;Knowledge distillation&lt;a class="headerlink" href="#knowledge-distillation" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;Knowledge distillation trains a student model to match outputs produced by a teacher. In conventional distillation, the teacher is often a larger, already trained network and the goal may be model compression.&lt;/p&gt;
&lt;p&gt;DINO uses &lt;strong&gt;self-distillation&lt;/strong&gt; differently: teacher and student have the same architecture, and the teacher is created continuously as an EMA of the student. There is no separately pretrained teacher and no class-label supervision. The term describes the direction of the learning target, not compression into a smaller model.&lt;/p&gt;
&lt;h4 id="representation-collapse"&gt;Representation collapse&lt;a class="headerlink" href="#representation-collapse" title="Permanent link"&gt;🔗&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;Representation collapse occurs when a network maps different inputs to the same or nearly the same output. Agreement between views then becomes trivial but the features cease to distinguish images. DINO avoids this outcome through the combined training dynamics of its stop-gradient momentum teacher, centering, and sharpening.&lt;/p&gt;
&lt;script type="text/javascript"&gt;if (!document.getElementById('mathjaxscript_pelican_#%@#$@#')) {
    var align = "center",
        indent = "0em",
        linebreak = "false";

    if (false) {
        align = (screen.width &lt; 768) ? "left" : align;
        indent = (screen.width &lt; 768) ? "0em" : indent;
        linebreak = (screen.width &lt; 768) ? 'true' : linebreak;
    }

    var mathjaxscript = document.createElement('script');
    mathjaxscript.id = 'mathjaxscript_pelican_#%@#$@#';
    mathjaxscript.type = 'text/javascript';
    mathjaxscript.src = 'https://cdnjs.cloudflare.com/ajax/libs/mathjax/2.7.3/latest.js?config=TeX-AMS-MML_HTMLorMML';

    var configscript = document.createElement('script');
    configscript.type = 'text/x-mathjax-config';
    configscript[(window.opera ? "innerHTML" : "text")] =
        "MathJax.Hub.Config({" +
        "    config: ['MMLorHTML.js']," +
        "    TeX: { extensions: ['AMSmath.js','AMSsymbols.js','noErrors.js','noUndefined.js'], equationNumbers: { autoNumber: 'none' } }," +
        "    jax: ['input/TeX','input/MathML','output/HTML-CSS']," +
        "    extensions: ['tex2jax.js','mml2jax.js','MathMenu.js','MathZoom.js']," +
        "    displayAlign: '"+ align +"'," +
        "    displayIndent: '"+ indent +"'," +
        "    showMathMenu: true," +
        "    messageStyle: 'normal'," +
        "    tex2jax: { " +
        "        inlineMath: [ ['\\\\(','\\\\)'] ], " +
        "        displayMath: [ ['$$','$$'] ]," +
        "        processEscapes: true," +
        "        preview: 'TeX'," +
        "    }, " +
        "    'HTML-CSS': { " +
        "        availableFonts: ['STIX', 'TeX']," +
        "        preferredFont: 'STIX'," +
        "        styles: { '.MathJax_Display, .MathJax .mo, .MathJax .mi, .MathJax .mn': {color: 'inherit ! important'} }," +
        "        linebreaks: { automatic: "+ linebreak +", width: '90% container' }," +
        "    }, " +
        "}); " +
        "if ('default' !== 'default') {" +
            "MathJax.Hub.Register.StartupHook('HTML-CSS Jax Ready',function () {" +
                "var VARIANT = MathJax.OutputJax['HTML-CSS'].FONTDATA.VARIANT;" +
                "VARIANT['normal'].fonts.unshift('MathJax_default');" +
                "VARIANT['bold'].fonts.unshift('MathJax_default-bold');" +
                "VARIANT['italic'].fonts.unshift('MathJax_default-italic');" +
                "VARIANT['-tex-mathit'].fonts.unshift('MathJax_default-italic');" +
            "});" +
            "MathJax.Hub.Register.StartupHook('SVG Jax Ready',function () {" +
                "var VARIANT = MathJax.OutputJax.SVG.FONTDATA.VARIANT;" +
                "VARIANT['normal'].fonts.unshift('MathJax_default');" +
                "VARIANT['bold'].fonts.unshift('MathJax_default-bold');" +
                "VARIANT['italic'].fonts.unshift('MathJax_default-italic');" +
                "VARIANT['-tex-mathit'].fonts.unshift('MathJax_default-italic');" +
            "});" +
        "}";

    (document.body || document.getElementsByTagName('head')[0]).appendChild(configscript);
    (document.body || document.getElementsByTagName('head')[0]).appendChild(mathjaxscript);
}
&lt;/script&gt;</content><category term="Computer Vision"/><category term="Representation Learning"/><category term="Transformer"/><category term="CV"/><category term="Vision Transformer"/><category term="ViT"/><category term="Self-Supervision (SSL)"/><category term="k-NN"/></entry></feed>